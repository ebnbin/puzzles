# 按键适配

四十个谜题的屏幕按键:每个游戏读哪些键、给了什么按钮、还差什么。改动落地时同步改这里
——**这不是生成物**,没有脚本会替你更新它。设计过程、被否决的方案、测量记录都在 git 历史里,
这份文件只记现状、判据和坑。

## 判据

三条,先后关系不能颠倒。

**一、`need` 键:一个键值得做成按钮,当且仅当它做的那件事没有任何手势能做到。**
不是「有用」,不是「常用」,是「够不着」。

- 「够不着」有两个来源:上游没有那条路,和**我们没把那条路转发出去**(`usePuzzlePointer`)。
  第二种读上游读不出来——它不在 `interpret_move` 里。
- 判「够不着」之前把 `interpret_move` **整个**读完:键盘那半边、鼠标那半边、拖拽的松手分支、
  `switch` 掉下去的地方。历史上四次错判,三次那条够得着的路就在同一个函数里。
- 读到带条件的拒绝,先问那个条件什么时候不成立;「读不到」不等于「办不到」——`move_cursor`
  夹边不绕回,连按若干次就能把光标推到已知位置,推导可以代替读取。
- 上膛键该不该给,问「这一排还有没有别的路」,不是「上游有没有这个修饰键」(pearl 的画线不给:
  拖拽到得了,按压更少;打叉给:拖拽产生不出 `M`)。

**二、`arrow` 和 `pick`:判「方向键开着时,这套自己成不成立」。** 没有它们
方向键就是装饰,所以和方向键同一个开关。棋盘上多一条手势不会让这一排里的键变多余
——「够不着」问的是有没有人能做,这一条问的是这套输入自己讲不讲得通,两个问题不互相抵扣。

**三、`arrow` 跟着上游的光标走,不把光标赶到我们挑的地方去。** 图标说的是「这个键现在
是什么」,不是「按下去会怎样」;这一侧答不出来的量(方向、位置)一点都不能出现在图标里——
画错比不画糟。加字形之前先把 `Icon.tsx` 里已占的形状普查一遍,撞脸没有任何检查会报;
候选按实际渲染尺寸(20px 亮、20px 暗、56px)并排看过再定;方向键旁边的图标不能长得像箭头。

## 五类按钮 · 两个区域

**用名字称呼,不要用编号。** 编号在这份文档里出现过三套互相冲突的版本,加一类就得
把后面全部往后挪一位——名字不会。五个名字的定义在 `src/engine/types.ts`,那是 SSOT。

底部分两个区域:**上方区域**是 `.keypad`(棋盘正下方,按游戏发牌),**下方区域**是
`.play-actions`(位置固定,一辈子长一个样)。

| 名字 | 谁 | 在哪 | 什么时候在 | 长什么样 |
| --- | --- | --- | --- | --- |
| `act` | Undo / Redo / Type / Menu | 下方 `.play-acts` | 总在 | 扁平,只剩图标 |
| `need` | 数字、`⌫`、Undead 的三个怪物 | 上方 `.keypad` | 总在 | 实心键面,无边框 |
| `arrow` | 方向键本体、`Enter`/`Space` 伴生键、slant 那种绝对键 | 下方 `.play-arrows` | `puzzles.arrows` | 白键面 + 边框,正圆,整簇坐在底板上 |
| `pick` | Map 的四个区域色、Guess 的颜色钉 | 上方 `.keypad` | `puzzles.arrows` | 同 `arrow`:一样的键面、一样的正圆 |
| `aid` | 提示、`M` 铺标记、三个标记键、`J` 重排、Dominosa 的高亮数字 | 上方 `.keypad` | `puzzles.aid` | 键面同 `arrow`,形状是圆角矩形,字色淡一档 |

**布局。** 下方两块位置写死(`act` 在左、`arrow` 在右),上方按 **`need` → `pick` →
`aid`** 排。这个顺序是结构不是约定:`keysFor` 出口按 `KeyKind` 那个联合的顺序稳定排序,
每条 `RULES` 内部的次序原样保留,所以规则里写乱了也排得对。

**`pick` 为什么不跟 `arrow` 住一起。** 它和 `arrow` 是一伙的——同一个开关、同一套键面、
同样的正圆——但方向键块封死在 3×3,Guess 最多十种颜色装不下(见「现在的形状」)。所以
它挤在上方区域里,和邻座的 `need`、`aid` 靠**圆**分开:那两类都是圆角矩形。

**只有两种键面**:有边框的填 `--surface`(`arrow` / `pick` / `aid`),没边框的填
`--surface-2`(`act` / `need`)。白在 `--bg` 上只差 2%,所以 `aid` 读出「这是个键」
靠的是那圈边框,不是填充;真正分开 `need` 和 `aid` 的也是边框。

**`need` 和 `aid` 的分界是「没有它这一局玩不完」**,不是「常不常用」:Dominosa 的数字只
高亮不落子,`M` 铺的是铅笔标记,一局不写标记照样能填完——都是 `aid`。这条判据在
`2802d6c` 收紧过一次(Pattern 的灰键从 `need` 改判成方向键方案的一部分,理由是解完的
棋盘上没有灰)。

**两个开关。** `puzzles.arrows`(见 `useArrows.ts`)关掉时 `arrow` 和 `pick` 一起不画:
整块 `.play-arrows` 不渲染,上方区域把 `kind === 'pick'` 的滤掉。Map 因此关了方向键就
没有上方区域(它的键全是 `pick`),Guess 只剩一个提示键。

`puzzles.aid`(见 `useAid.ts`)管 `aid`,**默认关**,和方向键一样是选进来的:提示、铺标记
这些一局不用照样玩得完(就是 `need`/`aid` 那条判据),所以默认的键盘只留必要键。整排都是
`aid` 的八个游戏(net、guess、dominosa、galaxies、fifteen、bridges、range、pearl)因此
默认没有上方区域,棋盘多一行。

## 现在的形状

方向键是**一个全局设置**,默认关,开关在首页的设置面板里(`puzzles.arrows`,见 `useArrows.ts`)。
开启后棋盘下方出现一排键:**固定键那四个在左,方向键在右**,位置固定,没有偏好可调。

方向键块**最多 3×3**。格子号从下往上、每行从左到右——**和屏幕的自上而下反着**;
没人注册的格子是个洞,没人注册的行整行不画,块就矮一截:

```
 7  8  9
 4  5  6
 1  2  3
```

**九格对四十个游戏够用**,前提是 Map 和 Guess 的颜色键不在这儿(它们是 `pick`,排在
上方区域,见「五类按钮」)。今天用到第三行的只有 pattern、slant、inertia、bridges 四个,一个都不
超过 9。**别为了塞下什么再往这儿加第十格**:上一版为了装 Guess 的十种颜色把它扩到过
13 格,代价是顶行要能从 3 格长到 7 格、满格那档还得把键窄成 36、缝减半成 2,触摸节距
从 44 掉到 38,而且那一行和下面两行对不齐。把颜色键挪出去之后这些全部消失。

**整块宽度 268**:

```
rim + 128 + 2×gap + 128 + rim = 6×tap-w + 7×gap = 268
方向键块和固定键块各 128(3×40+2×4 = 2×62+4),中间隔两道缝
```

两块**等宽**是 `--act-w`(`1.5×tap-w + gap/2` = 62)这个定义的全部用处;它们之间隔的是
**两道缝**(8),比键与键之间宽一倍——两块才读得出是两块。键 40、缝 4,**节距 44**,
全场只有这一档(Apple 那条线正好够)。底板 rim 是半道缝(2):撑满一道就和固定键贴上了。

每个游戏在 `pad.ts` 的 `PAD` 里把自己的键钉死在格子号上,**四个方向键也逐个游戏写全**:
默认 左=1 下=2 右=3 上=5,伴生键从 4、6 起往上排。

一格可以有两个租客(写成数组),谁的 `face()` 活着就是谁——Pearl 的 6 号格画线时是「打叉」、
收得回来时是「取消」,靠的就是这个。八向的游戏(只有 Inertia)把八个方向摆满一圈,
中间的 5 号格正好留给它的「一直滑到底」,所以**八向和伴生键不再互斥**。

Map 和 Guess 的方向键块里剩下什么:

- **Map** 1/2/3/5 四个方向 + `4` 是「可能」模式、`6` 是清空。四个区域色在上方区域。
- **Guess** **只有左右**(1、3),2 号格空着:上下在上游是选颜色的,那件事被上方区域
  那排圆键顶替了。`4` 保留、`5` 删除、`6` 看结果,全是上游自己的键,判据见下。

## 总览

| 状态 | 数量 | 谁 |
| --- | --- | --- |
| ✅ 完成 | 39 | 全部,见下表 |
| 🚫 不适用 | 1 | Loopy,只有鼠标 |
| ⬜ 待办 | 0 | |

**这张表数的是游戏,不是键。** 全表里剩下的 ⬜ 是「某个游戏里还差一个键」,不是这个游戏没做;
跨游戏的旧账、已知代价和会安静坏掉的地方,全部集中在「遗留问题」一节。

## 全表

「已有」= 今天就能按到(键盘上的按钮,或者一个手势)。「还缺」= 够不着的。

`Enter` / `Space` 那一列写的是这个游戏拿它们做什么。**35 个游戏的说法取自上游自己**——它们
实现了 `current_key_label`,会按当前光标位置返回一个活的词。五个没实现(`NULL`):cube、
fifteen、loopy 三个本来就不读这两个键,**untangle 和 palisade 读但不报**——词是我们自己起的,
见「机制」的 SILENT。

| 游戏 | 状态 | `arrow` / `pick`:光标那一套 | `need` / `aid`:独立键 |
| --- | --- | --- | --- |
| net | ✅ | `Enter` 左转 ✅ · `Space` 锁定/解锁 ✅(没光标时两个都置灰,靠一份副本)· **手指上**长按=锁定,右转用三次左转;**鼠标照上游原样** | `J` 重排 ✅ |
| cube | ✅ | —— 无光标,方向键即走子;**滚不过去的那个方向会置灰**(全集唯一一处,见「坑」) | —— 不读 `Enter`/`Space` |
| fifteen | ✅ | —— 无光标,方向键即滑块 | `h` 提示 ✅ |
| sixteen | ✅ | **完全跟随上游**,两个键各四张脸:格子上 `Enter` 粘住 Ctrl ✅ · `Space` 粘住 Shift ✅(两个都是「模式」);边框上 `Enter` 顺着箭头推一格 ✅ · `Space` 往回拉一格 ✅(图标不带方向)· 锁上时四个方向键也换成「推方块」· 光标想去哪去哪,见判据第三条 | —— |
| twiddle | ✅ | `Enter` 逆时针 ✅ · `Space` 顺时针 ✅(都够得着,纯粹是让方向键有意义,见判据) | `a`/`b`/`c`/`d` 转四角块⭕ · 小键盘转九个定位块⭕ |
| rect | ✅ | `Enter` 画矩形 ✅ · `Space` 清内部 ✅ · 拖拽中两个键变成「完成」和「取消」✅(灰勾落在开拖拽的那一侧) | —— |
| netslide | ✅ | `Enter` 推一行/列 ✅(`Space` 是同义词,只给一个键;反方向只有右键/长按,键盘够不着) | —— |
| pattern | ✅ | 黑 ✅ · 白 ✅ · 灰 ✅(三个键各自恒定,不换脸也不置灰,`lit` 全 app 唯一的幂等例外)· **第四个键是「刷」模式** ✅(粘滞修饰键,见「机制」) | —— |
| solo | ✅ | 数字 ✅ · `⌫` ✅ · `Enter` 切墨水/铅笔 ✅ · `Space` = 清除,和 `⌫` 同一分支 | `M` ✅ · 三个标记键 ✅ |
| mines | ✅ | `Enter` 翻开/清除周围 ✅ · `Space` 插旗/取旗 ✅(按钮跟着标签换脸;`Enter` 只在旗数对上时才亮)· 中键⭕点已翻开的格子就是连开 | —— |
| samegame | ✅ | `Enter` 选中/移除 ✅(一个键两张脸;选不了的格子上置灰)· 取消选中 ✅(`does: 'Unselect'`,第二层)· 需要一份光标副本 | —— |
| flip | ✅ | `Enter` 翻转 ✅(`Space` 是同义词;标签是个常量,所以按钮醒来之后永不置灰)· 需要一份光标副本 | —— |
| guess | ✅ | **左右两个方向键**(上下不给,选颜色那件事归上方区域的圆键)· `Space` 保留 · `\b` 清掉光标那颗 · `Enter` 看结果 ✅(三个键三种状态互斥置灰,见下)· **颜色钉在上方区域**,一按 = 上下顶到那个颜色 + `Enter`,落完光标不动 | `h`/`?` 提示 ✅ · `l` 标签开关⭕在偏好设置里 |
| pegs | ✅ | `Enter` 选中/取消 ✅(按下去是给方向键上膛,跳跃是下一次方向键;上了膛换成叉;孔位上置灰)· 不需要副本 | —— |
| dominosa | ✅ | `Enter` 放骨牌/拿走 ✅ · `Space` 画线/擦掉 ✅(半格网格,只有「正好一个坐标是奇数」的落点两个键才活)· 需要一份副本,只有方向键唤醒 | 数字高亮骨牌 ✅ |
| untangle | ✅ | `Enter` 拿起/放下一个点 ✅ · `Space` 循环到下一个点 ✅(后端一个字都不报,两个按钮永远可按,SILENT)· 不需要副本 | —— |
| blackbox | ✅ | `Enter` 发射/猜球/取消/检查 ✅(一个键四张脸)· `Space` 锁定/解锁 ✅ · 角上那张「检查」有一格亮着按不动,见「遗留问题」 | —— |
| slant | ✅ | `\` `/` `⌫` 三个键各发自己那个字面键 ✅(上游本来就有的绝对键;光标没显示时三个一起灰)· 循环键不再给按钮 | —— |
| lightup | ✅ | 两个开关:放灯 · 打叉 ✅(上游自己的绝对键;对方的记号上一按替换,两条走子)· 黑格上两个一起灰 | —— |
| map | ✅ | **「可能」修饰键(4)+ 清空(6)在方向键块;4 个颜色键在上方区域**(`pick`,圆键),全是我们的 ✅(上游没有任何键能说出一个颜色,走存档门,见「机制」)· 光标没醒、或站在线索上时颜色键和清空置灰 | `l` 显示区域编号⬜(偏好里已有 `show-numbers`) |
| loopy | 🚫 | —— | —— 只有鼠标;中键「未知」⭕点一下已画的线就是它 |
| inertia | ✅ | `Enter` = Advance(重放求解器下一步)✅ 九宫格正中心,按过求解才出现(`level: 2`) | —— |
| tents | ✅ | 两个开关:帐篷 · 草 ✅(脚下已经是自己那个值时改发 `B`;上游的词分不出帐篷和草,那一位读存档,`engine/tents`)· 树上两个一起灰 | —— |
| bridges | ✅ | 三个键:岛完成了(`Space`)· 架桥 · 没有桥 ✅(后两个是一次性上膛键;`Enter` 那套拖拽整个不发了)· 上膛时这一排只显示那一个键 | `h` 提示 ✅ · `g` 提示开关 ⚙️ 在偏好里 |
| unequal | ✅ | 数字 ✅ · `⌫` ✅ · `Enter` 切墨水/铅笔 ✅ · `Space` 同 `Enter` | `h` ✅ · `M` ✅ · 三个标记键 ✅ |
| galaxies | ✅ | `Enter` 七个词一个键 ✅(它的标签不看是哪个键,第二个键会是复制) | `h` 走一步显然推理 ✅(一次性:料在发牌时定死,第一按之后棋盘不再动) |
| filling | ✅ | 数字 ✅ · `⌫` ✅ · **十字这边一个键都没有**(上游三个选区键全不给:不用选区照样玩,`Space` 反而多一次按压;「取消选区」的传感器要靠读没分配的内存,不能用) | —— |
| keen | ✅ | 数字 ✅ · `⌫` ✅ · `Enter` 切墨水/铅笔 ✅ · `Space` = 清除 | `M` ✅ · 三个标记键 ✅ |
| towers | ✅ | 数字 ✅ · `⌫` ✅ · `Enter` 切墨水/铅笔 ✅ · `Space` = 清除 | `M` ✅ · 三个标记键 ✅ |
| singles | ✅ | 两个开关:涂黑 · 画圈 ✅(全从标签解出来,一个字节不记)· 圈↔黑一按(同一个键发两次:先擦对方再放自己,两条走子) | —— |
| magnets | ✅ | `Enter` +→−→空 ✅ · `Space` 空白骨牌→两个问号→空 ✅ | —— |
| signpost | ✅ | 闲着:连到下一格 · 连到上一格 ✅ · 连接开着时分成勾和叉,一次只有一个能按(勾在开它的那个键上)· `x` 拆这一格⭕拖出棋盘同一步 · `X` 拆整条链⬜ | —— |
| range | ✅ | 两个开关:涂黑 · 打点 ✅(三态三对不同词,全从标签解出来)· 线索格上一起灰 | `h` 提示 ✅ |
| pearl | ✅ | 两层各两个键:闲着「起笔 · 打叉」(叉是一次性上膛),画着「停笔 · 放弃」(一格没走时勾是灰的,自记一位)· `Ctrl` 画线不给 | `h` 提示 ✅ |
| undead | ✅ | 怪物 ✅ · `⌫` ✅ · `Enter` 切墨水/铅笔 ✅ · `Space` = 清除 | `M` ✅ · 三个标记键 ✅ · `a` 图片/字母(偏好里也有) |
| unruly | ✅ | 两个开关:黑 · 白 ✅(六个转移每个一按,黑↔白也是,一个字节不记)· 线索格上一起灰 · `0`/`1`/`2` 直接设⭕ | —— |
| flood | ✅ | `Enter` 淹没左上角 ✅ · `Space` 重放求解器下一步 ✅(按过求解才出现;这个键不看光标,`offCursor`)· 需要一份副本 | —— |
| tracks | ✅ | 两个开关:铺轨 · 打叉 ✅(上游自己的绝对键;对方记号上一按替换,替换失败自动撤回)· 线索格两个一起灰 | ——(`h` 在 `#if 0` 里,死代码) |
| palisade | ✅ | 两个开关:画墙 · 标记没有墙 ✅(后端一个标签都不报,键的死活从画面里读,SILENT)· 死落点上两个一起灰 · 对方记号上一按替换 · 只在 Half-grid 光标模式下给按钮(`READS_PREFS`) | —— |
| mosaic | ✅ | 两个开关:黑 · 白 ✅(和 unruly 逐字同一套)· 解完之后两个一起灰(上游自己不报了) | —— |

⭕ = 碰棋盘就能做到,按判据一不该做成按钮。

## 遗留问题

四十个游戏走完之后剩下的账,一处集中。「还没做」「审计过决定不做」「做了但有代价」「以后会
安静地坏掉」是四件不同的事。每条都写卡在哪——一条没有卡点的遗留问题其实是没做,应该直接做掉。

### 一、还没做

| 谁 | 什么 | 卡在哪 |
| --- | --- | --- |
| 修饰键组合 | net 移原点/源、unequal 和 towers 划线索、palisade 画墙 | 机制已有(粘滞如 pattern 的刷、一次性如 bridges),剩「一个一个铺」。铺之前先分清是够不着还是只是快一点,再翻一遍上游手册——sixteen 那种上游自带的粘滞版本比造一个便宜 |
| signpost 的 `X` | 拆整条链 | 要一次右键拖拽,长按变拖拽的路已通;探过一次没写出走子,没查清是拖错了地方还是真不通 |
| map 的 `l` | 显示区域编号 | 偏好设置里已经有(`show-numbers`),剩下的只是「要不要再给一个按钮」 |
| `wouldSend` 的返回类型 | 把「为什么不发」说出来 | null 有四个来源(`gated && !awake`、`does` 无人提供、`faces` 无脸、`doesNothing`),一个 bit 盖不住,已让 pattern 的 `lit` 出过一次「没光标也全亮」的 bug。修法是返回带原因的联合类型,`lit` 变成「already 时仍然亮」 |

逐游戏推进看不见机制缺陷:每个 entry 单独看都对,只有问「这个 null 到底什么意思」才看得见。
表是数据,逐游戏加是对的;**机制不是数据,不会因为多加一个游戏而被复查。**

### 二、审计过,决定不补(2026-08-14,前提是上游 `3c36322`)

四十个游戏逐个对照过上游全部输入(输入表见 `docs/inputs.md`,每条带源码行号):
**真够不着的只有 3 处,没有一处会改棋盘**——

- **Untangle** 反向循环顶点(`Shift+Tab`):只是绕远,正向 `Space` 在。
- **Net** 平移视野/移动信号源(`Shift`/`Ctrl`+方向):纯显示,不改解答。
- **Filling** 取消整个选区(`Esc`):我们根本不给选区键,这个状态进不去(见全表 filling)。

另有两处能做到但藏得深(Singles 点棋盘外切换黑格数字、Undead 长按计数区换样式):显示开关,
不值一个键位。快捷方式类(上游更快的入口,同一件事换个方式到得了)按定义不补。
**重新打开任何一条,得先有一条能改棋盘的**;这些决定只对这个上游 commit 成立。

### 三、明知有代价,选择了不修

| 谁 | 代价 | 为什么不修 |
| --- | --- | --- |
| blackbox 角上的「检查」 | 摆够球、走到角上、撤销一枚后,按钮亮着按下去无事(上游标签比行为宽一格:`current_key_label` 在角上无条件报 Check,干活的分支查 `CAN_REVEAL`) | 两条修法一条引入「读状态栏散文」的新依赖、一条是 `engine/marks` 量级的自己数球;撞上的代价只是白按一下 |
| netslide / sixteen 的边框 | 光标在边框上时四个方向键只有两个活,看起来像 bug | 上游键盘本来就这样;标签整圈报同一个词,分不出光标在哪条边,没有依据置灰 |
| mines 的 `Enter` | 亮 ≠ 安全:旗插错了照样亮,按下去踩雷 | 风险是这步棋本身的,点数字承担的完全相同;但记住,一个亮着的按钮比一次点击更像背书 |

### 四、会安静地坏掉的

坏的时候没有任何东西报错,`npm run build` 照样绿。

1. **`key()` 的返回值只对拼作 `Backspace` 的键有意义**(emcc.c 给 KaiOS 的照顾)。guess 的 `⌫`
   靠它向后删;上游删掉那个分支,这个键悄悄退回上游语义。
2. **`lsk === csk` 的抹空**:两键同词时第一个被抹成空串,「空」和「被抹掉的」分不出来。`BOTH`
   是唯一的解药,而它是手写的——每加一个用 `faces` 的游戏,先把它 `current_key_label` 的所有
   分支列一遍。漏了不报错,只会让一个按钮承诺它做不到的事(dominosa 现过形)。
3. **这一块没有提交进仓库的自动检查**:正确性是「按钮的死活和后端对不对得上」,只有真跑引擎
   才验得了(check 脚本之外的验证都是临时搭的)。改这一块别只信 build 绿。
4. **`BOTH.bridges` 疑似已无消费者**:`BOTH` 只服务 `faces`,而 bridges 现在没有 `faces` 键。
   删之前核实一遍。

这份文档自己也是第五处:手写的,只能手写着改。教训两条——grep 不是阅读(可见性标志十种拼法、
`IS_CURSOR_SELECT` 宏、fallthrough 都数不出来);读到带条件的拒绝,先问条件什么时候不成立。

## 机制

跨游戏的机制分两处:上方区域那排在 `keys.ts`,方向键那块在 `pad.ts`。下面记每个机制
「是什么 + 不知道会用错的那一句」。

**标签通道。** `current_key_label` 按光标位置返回两个词,经 `onKeyLabels` 进来,这是这一侧
唯一能知道的关于光标的事(`midend_get_cursor_location` 没被 emcc.c 导出)。`post_move` 是
同步的:`api.key()` 一返回标签就已更新,但 React 还没渲染——**要读 ref,不能读 state**。

**抹空与 `BOTH`。** 两键同词时上游把第一个抹成空串,所以 `labels.space !== ''` 恰好等于
「两个键干的事不同」——第二个按钮值不值得给,这一条白送。被抹掉的词由 `BOTH`(每游戏「两键
可能同时报出的词」)还原,现在三条:rect、bridges、signpost。`BOTH` 只服务 `faces`。

**`faces`(换脸键)。** 同一个键在一局里换工作,脸跟着标签走。置灰规则比普通键强:**当前词
不在脸谱里就灰**,哪怕按下去真会发生什么;灰的时候戴自己的默认脸,不是别人的脸。多张脸成立
的判据是「同一份工作在不同地方的样子」(blackbox 四张脸都是「把脚下这格该做的事做掉」),
samegame 的「取消选中」不是这个键的工作,所以走 `SECOND` 另开一层。

**`does` + `instead`(按结果命名)。** 按钮的名字是结果,由标签反查此刻哪个键给出它——
pattern 用它把两个循环键翻成三个位置钉死的颜色键,samegame 用它让「取消」自己知道何时该灭。
`instead`:要不到自己那个词时改要「清空」,「没有人提供我要的词」恰好等于「这一格已经是它」。
**tracks 是反例**:它的 `Clear` 两个键都报、含义不同(清轨/清叉),`does` 会擦错东西,只能用
`faces`。

**`lit` / `silent` / `doesNothing`。** `doesNothing`(两词皆空)是标准置灰;`lit` 是 pattern
三个颜色键的幂等例外(全 app 唯一:刷到一半按钮不许在拇指底下熄灭),`silent`(两词皆空 =
光标没显示)把「幂等」和「没光标」分开——`lit` 只豁免前者。

**`SECOND` / `level`(两层菜单)。** 这一排是「当前这一层的菜单」,不是固定清单。`level: 2`
的格位只在第二层出现;`SECOND` 声明哪些词表示第二层开着,现在四条(samegame、inertia、flood、
pearl)。**写进去的词必须对两个键都只属于第二层**(flood 的 Advance 只在 `Space` 上,只读
`Enter` 的词曾让它永远消失)。第二层的语义是「光标正站在待定物上」,不是「有一个待定物存在」
——前者是标签的纯函数,后者要记状态;光标睡着时不算在层里。排版不数这些:格子号是注册时钉死的
(`pad.ts`),同层换人的两个键注册在同一格,常在的键永不挪位。

**上膛键。** 按下去不落子,下一个方向键带修饰键发出去;上膛时这一排只显示它,再按一次退出。
**只在真落了子之后才消耗**——上游在移动失败时不清 `dragging`(bridges 撞边界那一按什么都不写
却把引擎留在拖拽里),所以要看存档字节判断落没落。被藏起来的键留在 DOM 里用 `visibility`
藏(iOS 上卸载再插回会画成残片)。

**粘滞模式。** pattern 的刷、map 的「可能」:按下亮着,亮着时方向键/颜色键带上修饰。模式改
的是**别的键**的行为,所以那些键要自己换脸(箭头尾巴加填实格、名字变「向上涂」);选中的
笔刷用环标记,**环只在有一支以上笔刷时画**(tents/range 只有一支,环会长得像一个按不动的
选择)。光标不在的空档:方向键裸发(第一下只唤醒)、模式键灭而位留着。

**替换(对方记号上一按)。** lightup/tracks/palisade/singles:先发对方的键擦掉,再发自己的,
一按两条走子、撤销要两次。lightup 的第二下**必然**落得下(能挡它的只有黑格,而黑格上邻居
一个字不报);tracks 保证不了(边的出口可能已占满),所以发完要**重读一次标签**,落不下就
`undo` 撤回——代价是那次撤回会点亮重做键。

**光标副本(`awake` 等)。** 默认是零:这一侧的东西全是推导,不是记录。加一份自留状态要过
三条,缺一不加——上游确实没有接口;输入面是封闭的(每一次按键、触摸都经这一侧);失效自纠正
(猜错一两步内自己回正轨,这条 `marks/` 过不了,所以那边是「拒绝」不是「记」)。现状:

- **`awake`(一个 bit)六个游戏**:net、samegame、flip、guess、dominosa、flood——四十个的
  `current_key_label` 逐个读过,只有这六个实现了却不查自己的可见性标志(标志有十种拼法,
  grep 数不出来,只能读)。清灭接在 `game_id_change_notify`(即 `onPermalinks`)上,新游戏/
  换预设/手打 id/读档/标记键 loadGame 一行全盖。有副本的游戏,按钮睡着时戴默认脸。
- **flood 的 `Advance` 带 `offCursor`**:它不在光标那儿干活,副本的门是按键开的,不是按游戏。
- **rect 的 `opened`(哪个键开的拖拽)**:四个状态里三个标签自己说得清,只有「开了没动」靠记,
  猜错免费(那一格两个键本来都是放弃)。
- **pearl 的一位**(起笔后走没走过格):标签说不出,存档不带;错的那个角(贴边起笔按朝外)
  代价是勾亮着按下去等于旁边的叉。
- **map 的光标坐标**(唯一一份坐标):调色板要往光标那格落色。敢记是因为 `midend_deserialise`
  每次重建 `game_ui`、光标回原点,**每按一次调色板副本就重新对齐一次**。

**SILENT(豁免,不伪造)。** untangle、palisade 的 `current_key_label` 是 `NULL`,照字面读
两个按钮会整局灰着,所以 `wouldSend` 见到这两个名字直接放行。untangle 免费(它的 `Enter`
每条分支都做事);palisade 的键从每帧画面里读死活(`engine/palisade`),Full-grid 模式下
一个按钮都不给——修饰键发不出去,两个永远无效的亮键不如没有,所以它在 `READS_PREFS` 里。

**map 走存档门。** 上游没有任何键能说出一个颜色,四个颜色键从存档门写走子(`<颜色>:<区域>`)。
区域编号**问引擎,不算几何**——第一版重写了 map.c 的几何,错在对角线格子依赖 SHA-1 洗牌的
访问顺序,一局约 1% 的格子错、错得很安静。现在先把每个区域刷上颜色再让引擎自己说(全涂色
让「沉默」只剩「这是线索」一个意思)。代价每按一次 70–120ms。

**存档门的只读半边。** tents 的「脚下是什么」(标签分不出帐篷和草)从走子表读(`engine/tents`,
按 `STATEPOS` 截到当前位置);数字键角标的「还剩几个」同理(`marks/remaining`)。只读不写:
`game_ui` 不重建、光标不丢、照闪。

**`HOLD_BUTTON` 只管长按,鼠标一律照上游。** 名单里只有 net(长按=锁定):手指只有两下,鼠标
有三键加修饰,两者只在硬件不一致的那一处不一致。往名单里写和默认值相同的记录,名单就开始说谎。

**guess 只自己造了一样东西:一键选色。** 别的全部照上游,一个按钮对应上游自己的一个键,
脸和死活一律读 `current_key_label`。唯一的替换是那排圆的颜色钉:**一按 = 上下顶到那个颜色
+ `Enter`**,正是上游「上下选色、确认落子」那条路缩成一个键(`move_cursor` 夹边不绕回,
连按 `ncolours` 次「上」必到第一格——推导,不是记忆;取近的那一头顶)。所以落完光标**不动**,
和上游的 `CURSOR_SELECT` 一致。

上游另有一条路是数字键(落色**并前进一格**,`guess.c` 自己的注释就这么写),我们不用它:
两条路混着走会让「按一下到底动了什么」说不清。这里踩过一次:上一版把数字键、我们自造的
退格(`ArrowLeft` + `Backspace`)、一个自己数的 `typed` 计数器和提交后补的 `ArrowLeft`
拼在一起,结果是保留的钉子够提交时光标赖在「看结果」位、整排颜色键变哑弹,pad 上没有出路。
**拼接越多,参数一变就越容易出这种洞**——现在的规矩是不拼。

三个功能键的死活全由光标在哪定,一张表说完(`current_key_label` 报什么就是什么):

| 光标 | `labels.enter` / `.space` | 保留 | 删除 | 看结果 | 颜色钉 |
| --- | --- | --- | --- | --- | --- |
| 在钉子上 | `Place` / `Hold` | 亮 | 亮 | 灰 | 亮 |
| 在「看结果」位 | `Submit` / `""` | 灰 | 灰 | 亮 | 灰 |
| 解出来了 | `""` / `""` | 灰 | 灰 | 灰 | 灰 |

`Enter` 落在钉子上时上游是「放下调色板光标指着的那个颜色」,**这半边我们不给按钮**:
上方区域每个圆键都做同一件事(而且说得出是哪个颜色),按判据一它够得着。所以这个键
只剩「看结果」一张脸,光标不在那一位就是灰的。

颜色钉在「看结果」位一起置灰:那时候 `Enter` 是提交,不灰就成了「按颜色变成交卷」。按下去
之前还会再问一次 `labelsNow()`(上下只动调色板光标,钉子光标没动,所以顶完再问照样准),
挡的是状态刚变、按钮还没重画完那一瞬间的点击。颜色数超过 10 或者 id 认不出来时整排不画,
和别处一样——显示错的比不显示糟。

## 坑

- **cube 不是八向**:方向是每个格子自己声明的,正方形网格四个斜向全是 0,三角网格斜向是别名。
  inertia 才是真八向。
- **cube 的置灰分界**:棋盘自己会说的(顶到边界),我们不说;棋盘盖住了的(三角朝向被多面体
  压住),我们说。netslide/sixteen 的边框半活不置灰,理由相同的另一半——光标就摆在那儿。
  实现是推导不是副本(`engine/cube`,整数坐标),**升级上游后必须跑 `scripts/check-cube.mjs`**:
  模型漂了会把能按的键灰掉,错灰和该灰长得一模一样,读者报不上来。
- **死代码像功能**:tracks 的 `h` 在 `#if 0` 里,signpost 的 `h` 整段被注释掉。别照着加。
- **guess 的删除键在「看结果」位会越界**:`peg_cur` 的取值是 `0..npegs`,多出来的
  `npegs` 是「看结果」位,而 `curr_pegs->pegs` 只分配了 `npegs` 个。上游对这一位置
  防了 `CURSOR_SELECT` 和 `CURSOR_SELECT2`,**唯独 `'D'/'d'/'\b'` 那一支没防**——
  它会读 `pegs[npegs]`,读出来非零还会往那儿写个 0。`vendor/` 不能改,所以那一位
  必须把删除键灰掉,这是唯一一道拦;`labels.enter !== 'Place'` 就是那个传感器。
  写过一次实验:连按八次退格,四颗钉子一颗没变(正好反证打的是数组外面),没崩、
  没报错——内存错误的常态是看不见,看得见的时候在别处。
- **guess 提交完光标不会自己归位**:`game_changed_state` 只在「提交不了」时把光标从
  「看结果」位拉回来,保留的钉子本身够提交时它就赖在那儿。这不是死路——左方向键一按
  就出来,那是上游的走法;**别为此在提交后补 `ArrowLeft`**,那是我们替上游做决定。
- **一个按钮只有一份工作**:上游在某个局面拿同一个键干别的,按钮置灰而不是改行(samegame 的
  「选中」在孤立格上安静下来);但 sixteen 的边框推行不是别的工作——「工作」由上游这个键在
  这个位置上的意思定,不由我们定。
- **写用例的三条**:禁用的按钮仍戴默认脸,判死活问 `disabled` 不问文案;blackbox 按完「检查」
  光标自己跑回场地;pegs 点空孔不收光标(十字盘正中恰好是唯一的空孔)。
- **金丝雀**:只剩 cube 和 fifteen 没有光标键,而且是因为它们没有光标——用例里反着断言这一条。

六个 check 脚本(`check-cube` / `check-map` / `check-clues` / `check-palisade` /
`check-keys` / `check-solved`)何时跑、守什么,见各脚本和被测文件的头部注释。
