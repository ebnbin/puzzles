// docs/params.md 的手写源:逐游戏逐 string 参数的语义、上游规则(带行号)、本仓库的表与理由。
// build-params-doc.mjs 把它和 oracle 的控件表、模型算出的默认表、check-params 的结果拼成文档。
// 字段:u 上游规则(带行号),f 最终范围,d 依赖(谁看谁),c 上限来源
// (up=上游自身 / cap=CAP 100 / grid=棋盘≤100×100 推出 / sem=语义补的 / local=本游戏逐个定的),
// tuned 标记已经逐个读上游 + 实测定过范围的游戏(第五节的待办清单不再列它)。

export const GAMES = [
  {
    id: 'net', title: 'Net', file: 'net.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽(格)',
        u: ['宽高都 > 0(322)', '宽高不能同时 ≤ 1(324)', 'full 且 unique 且 wrap 时,宽或高都不能是 2(376)'],
        f: '3..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高(格)',
        u: ['同上'],
        f: '3..100', d: '—', c: 'cap' },
      { label: 'Barrier probability', kind: 'float', sem: '解上没走线的边里,画成实心墙的比例(nbarriers = 比例 × 候选边数,1534);上游文档把它写成提示旋钮',
        u: ['≥ 0(328)', '≤ 1(330)', 'atof 解析(314),%g 回显'],
        f: '0.00..1.00,步长 0.01', d: '—', c: 'up' },
    ],
    notes: [
      '实测:3..100 全域(宽 × 高 × wrap × unique × 101 档概率,3,880,016 组)逐组过 oracle,全部放行。',
      '生成耗时不构成约束——最贵的是 100×100 wrapping,原生 200 个种子平均 0.28 秒、最坏 1.35 秒;'
        + '偏长宽比反而更便宜(100×3 最坏 0.004 秒)。浏览器里从松手到新局画完最坏 534 毫秒。',
      '上限 100 以长边为准:2560×1440 上 100×50 是每格 25 px,读得清;同一局在手机上是每格 3 px,'
        + '这是用户自己拉出来的取舍。',
    ],
  },
  {
    id: 'cube', title: 'Cube', file: 'cube.c', tuned: true,
    params: [
      { label: 'Width / top', kind: 'int', sem: '方格:宽;三角格:六边形的顶边和两条下斜边的边长(d1),d2 = 0 时退化成三角形',
        u: ['d1, d2 ≥ 0(549)', '立方体(方格):两边都 ≥ 2(553)', '其它三种(三角格):两边不能都是 0(558)',
            '按 enum_grid_squares 逐格分类计数,每一类格数 ≥ 面数/类数(583-595):四面体 4 类、八面体 2 类、其余 1 类',
            '总面积 ≥ 面数 + 1(597):方格 d1·d2,三角格 d1²+d2²+4·d1·d2;面数 4 / 6 / 8 / 20'],
        f: '立方体 2..100;三角格 0..50。都再按上游的逐类计数和面积筛一遍(取「高放到最大时放得下」的那些)', d: '看「Type of solid」', c: 'local' },
      { label: 'Height / bottom', kind: 'int', sem: '方格:高;三角格:另三条边的边长(d2)',
        u: ['同上'],
        f: '同上的上限里,与当前宽一起放得下的那些。例:立方体宽 2 时高 ≥ 4,宽 3 时高 ≥ 3;四面体 (0,3)、(1,2) 可以,(1,1) 不行', d: '看立体类型和宽', c: 'local' },
    ],
    notes: [
      '分类计数在 cube.ts 里按上游逐行移植(roomFor),不是闭式公式。实测:四种立体的全部可达组合共 17,575 组逐组过 oracle,全部放行,没有空表;'
        + '「宽的表按高取满来筛」这条单调性假设也逐值验证过,没有 d1 被漏掉。',
      '两种网格的上限不同是因为铺展不同:六边形跨 d1+d2 行,三角格 50×50 和方格 100×100 的每格像素实测相当(1440×900 上 8.1 px / 7.1 px)。'
        + '格数则不等(15,000 / 10,000),而 game_redraw 是 O(面积) 且滚动带动画,所以每走一步的阻塞是 220 ms / 123 ms。生成本身全程 < 1 ms。',
      '方向键置灰的走位模型(cube.ts 的 parseParams)定义域跟着这两个上限走。以前它卡在 1..32:超过 32 的盘和所有三角形(d2 = 0)都没有置灰。'
        + '建网格 100–140 ms、按参数串缓存,之后每次观测 0.1 ms,抬限前后每步耗时没有可测差别。',
    ],
  },
  {
    id: 'fifteen', title: 'Fifteen', file: 'fifteen.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 2(149)', '宽×高不得溢出 int(151)'], f: '2..50', d: '—', c: 'local' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '2..50', d: '—', c: 'local' },
    ],
    notes: [
      '上限由字号定,不由格数定:格子里要写编号(最大 w·h−1),字号写死成格边的 1/3(fifteen.c:946)。'
        + '实测 8 px 字号是还认得出的底——50×50 在 2560×1440 上正好是每格 25 px、字号 8 px;'
        + '1440×900 上 30×30、手机上 15×15 也都落在这条线上。',
      '耗时不是约束:生成是 O(n²)(放置扫描 + perm_parity),原生 50×50 0.015 秒、100×100 0.28 秒;'
        + '走子只重画动的那几格,全尺寸 5–15 ms。DESC 是完整编号表,50×50 的存档 11.6 KB。',
    ],
  },
  {
    id: 'sixteen', title: 'Sixteen', file: 'sixteen.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 2(179)', '宽×高不得溢出 int(181)'], f: '2..50', d: '—', c: 'local' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '2..50', d: '—', c: 'local' },
      { label: 'Number of shuffling moves', kind: 'int',
        sem: '0 = 完全随机排列;N > 0 = 只做 N 次整行/整列滚动,状态栏显示「(target N)」,玩法是数出这 N 步再原样倒回去',
        u: ['≥ 0(183)', '无上限'], f: '0..100', d: '—', c: 'local' },
    ],
    notes: [
      '宽高的上限和 Fifteen 同据:格子里要写编号(最大 w·h),字号写死成格边的 1/3(sixteen.c:889),'
        + '边框还占整整一格(BORDER = TILE_SIZE),所以同格数下比 Fifteen 更小。50×50 在 2560×1440 上是每格 24 px、字号 8 px,压着「还认得出」那条线。',
      '打乱步数的上限不按「打乱够不够」定。实测错位比例:50×50 在 m=50 时才乱 59%,要追平 m=0(完全随机)得五千步以上;'
        + '10×10 要三五百步;4×4 二十步就到顶。但大 N 两头不靠——既不是能数出来的短打乱,又比 m=0 差(盘子只乱一半,'
        + 'target 还够不着,上游文档为此写了「N 越大,比 N 更短的解越可能存在」)。真要打乱用 0,那是默认值和全部五条预设的取值。',
      '耗时全程不是约束:原生 50×50 0.004 秒、100×100 0.090 秒;N > 0 时跳过 perm_parity 那条 O(n²) 路径,'
        + '100×100m10000 只要 0.002 秒。走子只重画滚动的那一行/列,4–10 ms。',
    ],
  },
  {
    id: 'twiddle', title: 'Twiddle', file: 'twiddle.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽 ≥ 旋转块边长 n(216)', 'n ≥ 2(214)', '宽×高不得溢出 int(220)'], f: '2..50', d: '主', c: 'local' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['高 ≥ n(218)'], f: '2..50', d: '主', c: 'local' },
      { label: 'Rotating block size', kind: 'int', sem: '一次旋转的方块边长;点一下把一个 n×n 块整体转 90°',
        u: ['≥ 2(214)', '≤ 宽且 ≤ 高(216-218)'], f: '2..min(宽, 高)', d: '看宽高;宽高缩小时被夹', c: 'up' },
      { label: 'Number of shuffling moves', kind: 'int',
        sem: '0 = 默认长打乱;N > 0 = 只做 N 次旋转,状态栏挂 target(语义同 Sixteen)',
        u: ['≥ 0(222)', '无上限'], f: '0..100', d: '—', c: 'local' },
    ],
    notes: [
      '宽高的上限和 Fifteen 同据(字号 = 格边/3,twiddle.c:1005)。块边长不额外收,只跟上游的 n ≤ min(宽, 高)——这是 owner 定的,'
        + '耗时留到第三轮:打乱步数为 0(默认值、全部八条预设)时上游走 w·h·n²·2 步、每步 do_rotate 是 O(n²)(twiddle.c:340),整体 O(w·h·n⁴)。'
        + '本仓库这台机器上逐档实测 50×50:n=10 落定 0.39 秒、n=20 三秒、n=30 9.5 秒、n=40 21.6 秒、n=50 140 秒。'
        + '**这些数字偏悲观**——测量机是一颗共享的 2.8 GHz Xeon,owner 在自己机器上实测同一组参数不超过 20 秒,差两三倍。',
      '耗时随 n 单调递增,没有中段峰值(20×20 与 30×30 的 n 全扫都逐档递增)。增速在顶端会变缓——prevmoves 的清零范围被可旋转位置数 (w−n+1)(h−n+1) 卡住,'
        + '有效指数从 4 掉到 3 左右——但从不掉头。',
      '唯一非单调的是 n = 宽 = 高:只有一个旋转位置,整场打乱退化成整盘 ±90° 的随机游走,净转动 ≡ 0 (mod 4) 时 grid_complete 成立、'
        + '外层把整场打乱作废重来,而防撤销的过滤器在这种情况下被显式关掉(twiddle.c:365)。同样 n=20、60 个种子:20x20n20 平均是无退化基准的 1.4 倍、最坏 6.3 倍,'
        + '而盘子更大的 21x21n20 最坏/平均只有 1.02。尾巴是几何分布、没有上界,所以顶上那一格的单次测量不是最坏值。',
      '打乱步数 > 0 时上面那条长打乱整个被跳过:50x50n25 要 8.4 秒,50x50n25m10 是 0 毫秒。所以耗时只出现在默认(m = 0)那条路径上。',
      '**定下来的**:宽高 2..50、块边长 2..min(宽, 高)、打乱步数 0..100,都是 owner 定的(上游对后两者只要求 ≥ 2 / ≥ 0,没有上限)。'
        + '打乱步数的 100 与 Sixteen 同据:它是「数得出来、能倒着走回去」那一档,不是「随机化」那一档——真要随机用 0(默认值和全部八条预设的取值)。',
    ],
  },
  {
    id: 'rect', title: 'Rectangles', file: 'rect.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 > 0(223)', '面积 ≥ 2(227)', '宽×高不得溢出 int(225)'], f: '2..100', d: '—', c: 'local' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '2..100', d: '—', c: 'local' },
      { label: 'Expansion factor', kind: 'float',
        sem: '先在一张 base 棋盘上把矩形铺满,再插行插列撑回原尺寸(矩形个数和相邻关系不变,只是每个变大)。base 边长 = ⌊边/(1+e)⌋,不足 2 时钉 2(1165-1168);0 = base 就是棋盘本身。界面上枚举的是 base 对,e 由它算出',
        u: ['≥ 0(229)', '无上限', 'atof 解析,%g 回显'],
        f: '把 (base_w, base_h) 的每一种枚举出来,一对一档;写进控件的是算出来的 e。档数随棋盘变(7×7 六档、100×100 九十九档、100×99 一百九十五档 = 全局上限)', d: '看宽高', c: 'local' },
    ],
    notes: [
      '**定下来的**:宽高 2..100(上限 100×100 是 owner 定的,和网格类参数同据:大屏桌面按长边算,100 格还认得出);'
        + '扩展因子换成 base 对的枚举。下面几条是理由和实测。',
      '宽高从 2 起是**避崩**,不是调优:上游只查 w·h ≥ 2,放行 1×n;但钳位那句写的是 `if (bw < 2 && w >= 2)`,'
        + '宽是 1 时不触发,base 边长留在 0,扩展因子一动就死——真机上 1×7 往上加一档即 wasm abort'
        + '(random.c:275 `bits < 32` 断言),之后连 midend_serialise 都断言失败。两维都 ≥ 2 时 base 必 ≥ 2×2,294 组抽样生成全过。',
      '扩展因子的行为已逐条实测清楚:矩形个数只由 **base 面积**决定,与 base 形状无关'
        + '(面积 16 时 4×4 出 7.1 个、8×2 出 7.6 个、16×1 出 6.8 个);e 只通过 base_w / base_h 两个整数影响生成'
        + '(expandfactor 在 rect.c 里只出现在 1165、1167 两行),所以同一对 base + 同种子 = 逐字节相同的局面。'
        + '跨棋盘对齐粒度的公式:想要 base b1×b2,则 e ∈ ( max(w/(b1+1), h/(b2+1)) − 1 , min(w/b1, h/b2) − 1 ],'
        + 'base 为 2 的那一维不参与上界(钳位让它到 +∞)。验证过 base 6×6 在 6×6 / 12×12 / 24×24 / 48×48 上矩形数都是 11.0–11.2。',
      'base 缩到最小(2×2)之后 e 再大也逐字节相同,那一点是 e > max(宽, 高)/3 − 1(边长掉到 2 需要 边/(1+e) < 3),'
        + '100 宽的棋盘是 32.3。**e 因此是个跟着棋盘变的量**:同一个 e 在 7×7 上已经到底,在 100×100 上才走了 1/16。'
        + '而且 e 只通过那一对 base 影响生成,**落在同一对 base 上的 e 是同一个输入**——真机验过:7×7 种子 #1,'
        + 'e = 0.2173 / 0.3 / 0.39 / 0.4 的 DESC 逐字节相同,e = 0.41 跨到 base 4×4 才换局面。'
        + '上游自己也把 e 归为不持久的参数(encode_params 只在 full 时写它),等于承认它在 new_desc() 之后不再有意义。',
      '所以面板上枚举的就是 **base 对**,一对一档,不再有连续的 e。档数由公式给出:'
        + '`N = 宽 + 高 − 3 − C`,`C = max(0, gcd − j + 1)`,`j` 看 `min(宽/gcd, 高/gcd)`:等于 1 取 3、等于 2 取 2、≥ 3 取 1。'
        + '(推导:`⌊边/s⌋ = k` 成立于 `s ∈ (边/(k+1), 边/k]`,所以一维的分界点是 `s = 边/k, k = 3..边`;'
        + '`宽/k = 高/m` 当且仅当 `k = j·宽\'`、`m = j·高\'`。)短边整除长边(含正方形)时 N = 长边 − 1;'
        + '互质且短边 ≥ 3 时 N = 宽 + 高 − 4。100×100 以内:最少 1(2×2)、最多 **195(100×99,唯一)**、平均 95.6,'
        + '44% 的棋盘超过一百档。',
      '每一档的 e 是算出来的不是扫出来的:两维的分界点 `1+e = 边/k` 并起来,每个分界点取到它下面最近的可表示值'
        + '(区间左开右闭,往下取才落在里面;分界点本身和它下面一格都试,因为单精度除法正好落在分界点上时往哪边倒说不准),'
        + '再按 e 递增对 base 对去重。全部 4950 块棋盘对过账:**没有一块比公式少**,19 块比公式多一档——那是上游单精度除法'
        + '多切出来的一级,真机上确实是另一个局面(18×6 在 e = 0.2 处 `(float)18/1.2 = 14.999999`,base 14×5,'
        + 'DESC 与左右两档都不同)。另抽 398 块棋盘与密扫对账:不漏、严格递增、六位有效数字、无重复。',
      '**这一档的 SSOT 是 e**:存档的 CPARAMS、Game ID、C 侧的 `game_params` 里只有它;base 对是每次由宽高现算的一张表,不落盘。'
        + '`e → base` 是全函数、和引擎逐位一致;`base → e` 是从一对多里挑代表。所以 `base → e → base` 是恒等(表里每一档都是这么验出来的),'
        + '`e → base → e` 不是——`7x7e0.3` 落在 base 5×5 的区间里,而那一档的代表是 0.399998。今天连这一步规范化都不做:'
        + '表外的 e 直接回第一档。要保住 Game ID 的意图,规则得改成「先按 base 对认,认不到再回第一档」。',
      'e 只能带六位有效数字:上游用 `%g` 把它回显进参数框(196),多了回来就变样,再落定时会被当成表外值。'
        + '整数部分占几位小数就少几位,所以档位代表值最多六位小数。',
      '换棋盘时表外的 e 一律回到第一档(e = 0,base 就是棋盘本身),不吸到最近的一档——整套枚举都换了,'
        + '旧的 e 落在新表哪一档都没有意义。注意只改短边时,如果那个 e 在新表里仍然是某一档(长边的分界点没变),'
        + '它会留在原地:范围模型只看得见「值在不在表里」,看不见「谁变了」。',
      '耗时全部来自唯一解求解器,失败会把整局作废重生成,尾巴很长:原生 30 个种子,50×50 平均 0.28 秒最坏 0.75 秒,'
        + '70×70 平均 1.84 最坏 6.29,100×100 五个种子平均 10.5 最坏 15.0。关掉「Ensure unique solution」后 100×100 只要 0.027 秒。',
    ],
  },
  {
    id: 'netslide', title: 'Netslide', file: 'netslide.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽;中心格 cx = 宽/2 所在的那一列钉死不能滑(347、581)',
        u: ['宽高都 > 1(313)', '宽 > INT_MAX/高 时拒(315)'], f: '2..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高;cy = 高/2 那一行同样钉死', u: ['同上(313、315)'], f: '2..100', d: '—', c: 'cap' },
      { label: 'Barrier probability', kind: 'float',
        sem: '**墙占候选边的比例**,不是每条边的独立概率:`墙数 = (int)(b × 候选边数)`(627);候选边数是定值,不环绕 (宽−1)(高−1)、环绕 宽×高+1',
        u: ['0..1(317-320)'], f: '0.00..1.00,步长 0.01', d: '—(效果看宽高和「Walls wrap around」)', c: 'up' },
      { label: 'Number of shuffling moves', kind: 'int',
        sem: '0 = 默认打乱 2(宽−1)(高−1) 次滑动(574);N > 0 = 只做 N 次,状态栏挂「(target N)」(1780),玩法是数出这 N 步倒回去(语义同 Sixteen / Twiddle)',
        u: ['≥ 0(321)', '无上限'], f: '0..100', d: '—', c: 'local' },
    ],
    notes: [
      '**定下来的**:三个参数在前面的游戏里都有对应,这一轮没有新范围。宽高 2..100(上限同 Net / Rectangles 的管道类;'
        + '下限用上游的 2,**没有跟 Net 抬到 3**——Net 那个 3 是因为上游最小是 1、1×n 退化成一根直管,这里上游最小就是 2,'
        + '2×n 靠滑动仍然成题);墙的比例照抄 Net 的同名参数;打乱步数照抄 Sixteen / Twiddle。',
      '`Barrier probability` 背后是个**整数墙数**:候选边 = 生成树用不到的边,而格子恒是一棵生成树(`宽×高−1` 条边),'
        + '所以候选数是定值——不环绕 `(宽−1)(高−1)`,环绕 `宽×高+1`。公式推的,实测七组逐个对上(5×5=16、10×7=54、2×2=1、'
        + '20×13=228;环绕 5×5=26、10×7=71、2×2=5)。也就是同一个整数墙数上的所有 b 是同一个输入,和 Rectangles 的 e 同类(见 1.5 节);'
        + '这一轮维持 0..1 步长 0.01,与 Net 一致。上游还特意让墙的选取「加量不换旧的」:同种子下把 b 调大,新墙是旧墙的超集(616-624)。'
        + '**墙越多越简单**——预设里 b=1 标 easy、b=0 标 medium、b=0 + 环绕标 hard(163-173)。',
      '**耗时完全不是约束**(四十个游戏里第一个):没有求解器,生成 = 造一棵生成树 + 随机滑几下。原生每组 10 个种子:'
        + '100×100 默认(打乱 19602 步)25 ms,100×100 环绕 + 满墙 27 ms,100×100 打乱一百万步 0.56 秒,50×50 / 100×10 / 3×100 都 ≤ 5 ms。',
      '打乱程度实测(错位比例,20 个种子;约 90% 是随机基准——管形只有十几种,随机排列也会撞上一成):'
        + '5×5 在 m=20 已到 81%、m=50 到 86%(默认 32 步是 86%);10×10 m=50 到 86%(默认 162 步 90%);'
        + '50×50 m=100 到 70%(默认 4802 步 92%);100×100 m=100 只到 54%、m=500 到 85%(默认 19602 步 92%)。'
        + '比 Sixteen 混得快得多——一次滑动就动一整行/列。',
      '小盘不出事:2×2 只有一行一列可动(中心行列钉死),上游那个「被拒就重抽」的打乱循环在这种盘上照样终止'
        + '(异侧方向的移动永远不会被判成撤销或重复)。2×2 / 2×3 / 3×2 各自加环绕、加满墙、m=1、2×100、100×2 环绕满墙,'
        + '15 组 × 10 个种子全部正常返回。上游**没有** Net 那条「环绕 + 宽或高为 2 就拒」的检查(那是 Net 唯一解求解器的要求,Netslide 没有求解器)。',
      'Game ID 里 `wrapping` 和 `movetarget` 是持久参数(不带 full 也编码,251-254),`barrier_probability` 只在 full 时写(249)——'
        + '和 Rectangles 的 e 同一待遇:上游认为墙的比例只在生成时有意义。',
    ],
  },
  {
    id: 'pattern', title: 'Pattern', file: 'pattern.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 > 0(184)', '宽或高 > INT_MAX−1、宽 > INT_MAX/高 时拒(186-188)', '面积 ≥ 2(189)'], f: '5、10 … 50(5 的倍数,10 档)', d: '—', c: 'local' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上(184、186-188、189)'], f: '同宽', d: '—', c: 'local' },
    ],
    notes: [
      '**定下来的**:宽高都只给 **5 的倍数 5..50**,**不设面积上限**——顶格 50×50 明知要几十秒也放行(owner 定的,见最后一条)。'
        + '上游五条预设 10×10 / 15×15 / 20×20 / 25×25 / 30×30 和默认的 15×15 全在刻度上,步长 5 不会让预设脱靶。',
      '生成是**拒绝采样**(`generate_soluble`,667-726),一轮三步,任何一步不过就整轮重来、没有次数上限:'
        + '① 造图(`generate`,251-343):每格一个随机数 → 一次 3×3 邻域平均 → 取**中位数**当阈值,正好一半格子涂黑;'
        + '② **不许有整行或整列纯色**(686-713),但该维 ≤ 2 时跳过,上游注释:「otherwise nothing will ever be successfully generated」;'
        + '③ **只靠单行单列推理就要能唯一确定**(`solve_puzzle`,510-665):反复调 `do_row` 推到不再变化,还剩一个 UNKNOWN 就算失败,不猜不回溯。'
        + '所以耗时 = 每轮成本 ÷ 接受概率,而**两头各有一条判据会塌**。',
      '**失效方式一:短边小 → 死在第 ② 步。** 一行 w 格整行纯色的概率 q 实测:w=3 → 0.60、4 → 0.42、5 → 0.30、6 → 0.21、'
        + '8 → 0.11、10 → 0.056、15 → 0.010、20 → 0.0017、≥25 → ~0。整盘要**每一行都不纯色**,概率 ≈ (1−q_短边)^长边:'
        + '**底数由短边定、指数是长边,与面积无关**。3×30 只有 90 格,概率 5×10⁻¹³,跑满 300 秒不出结果;10×100 有 1000 格,'
        + '概率 3×10⁻³,只要 0.1–0.5 秒。q 为什么这么大:一行 3 格若是独立抛硬币纯色概率只有 0.25,是那次 3×3 平均(上游**故意**'
        + '抹出成片黑白块)加上中位数阈值把它抬到 0.60 的。上游的 `w > 2` 守卫等于承认这个问题,只是门槛定低了。'
        + '(纯色行彼此不独立:每盘纯色行数的方差是二项分布的 1.4–1.6 倍,所以真实接受率比 (1−q)^h 高 3–200 倍,越稀有差得越多。)',
      '**失效方式二:面积大 → 死在第 ③ 步。** 求解器通过率随**面积**指数衰减、每轮成本只随面积线性长,'
        + '实测 20×20 43%、30×30 10%、35×35 2.7%、40×40 0.27%、44×44 0.067%、≥46×46 一千五百轮零通过,'
        + '拟合下来**面积每多约 100 格,耗时翻倍**(以 40×40 = 0.2 秒为锚,20×50、45×45、50×50 都对得上,散布约两倍)。',
      '逐档实测(本机;owner 的机器快两三倍):5×30 = 0.17 秒、5×50 = 11 秒;6×46 = 0.53、7×46 = 0.015、8×46 = 0.007;'
        + '20×50 = 0.011、30×50 = 0.15、40×40 = 0.20、40×50 = 6.2、44×44 = 2.0、45×45 = 2.7、46×46 = 8.4、47×47 = 13.4、'
        + '48×48 = 44.7、**50×50 平均 >70 秒**(五个种子 15 / 42 / 76 / 116 / >120)。5 的倍数网格上 55 个组合里,'
        + '除 45×50(约 18 秒,外推)和 50×50 外都在 11 秒以内。',
      '**顶格 50×50 是明知的代价**:本机平均 >70 秒、owner 的机器上 25–35 秒起,超过「20 秒」那条线,而且拒绝采样是几何分布、'
        + '尾巴没有上界。owner 在看过这张表之后仍选择不设面积上限,理由是规则简单;'
        + '要收的话最省事的一条是「面积 ≤ 2100」,在这个网格上正好只砍掉 45×50 和 50×50 两格。',
      '下限 5 也是踩着悬崖定的:6×50 要 0.9–1.2 秒,5×50 要 11 秒(差十倍),4×50 与 3×30 直接跑不出来。'
        + '上限 50 另有可读性一条:线索是文字,画出来是 `1.2×长边 + 4` 格(线索区 `TLBORDER(d) = d/5 + 2`,35、42-43),'
        + '长边 50 时 1440×900 桌面上每格 14 px、线索字约 8 px,和 Fifteen / Sixteen 封 50 同一量级;'
        + '长边 100 那种长条虽然生成只要 0.1 秒,每格只有 7 px、字 4 px,是被可读性砍掉的,不是被耗时。',
    ],
  },
  {
    id: 'solo', title: 'Solo', file: 'solo.c',
    params: [
      { label: 'Columns of sub-blocks', kind: 'int', sem: '子块的列数 c;阶数 = c·r',
        u: ['c ≥ 2(516)', 'c, r ≤ 255(518,ORDER_MAX)', 'c·r ≤ 31(520)', 'Killer 时 c·r ≤ 9(522)', 'X 时 c·r ≥ 4(524)',
            '勾了 Jigsaw:custom_params 先令 c := c·r、r := 1(502),所以「c ≥ 2」落在乘积上'],
        f: '勾了 Jigsaw:1..31(Killer 1..9);没勾:2..15(Killer 2..4),给行数 ≥ 2 留位', d: '看 Killer、Jigsaw', c: 'up' },
      { label: 'Rows of sub-blocks', kind: 'int', sem: '子块的行数 r;r = 1 即 Jigsaw 布局(4104)',
        u: ['上游没有查 r 的下限(0、负数都放行,生成时才出事)', '其余同上'],
        f: '勾了 Jigsaw:⌈2/c⌉..⌊31/c⌋(Killer ⌊9/c⌋);没勾:2..⌊31/c⌋;X 时从 ⌈4/c⌉ 起;二阶(c·r = 2 或 2×2)配 4 向旋转 / 4 向镜像 / 8 向镜像或 Killer 时去掉', d: '看列数、X、Killer、Jigsaw、Symmetry;列数变大时被夹', c: 'up' },
    ],
    notes: [
      '勾 Jigsaw 提交后,C 侧把两数相乘、行数归 1,再次打开面板看到的是 (c·r, 1)——和上游桌面版一样。Jigsaw 模式下再拉行数会让阶数成倍增长,这里不拦(上游放行)。',
      '没勾 Jigsaw 时行数从 2 起,是因为上游把 r = 1 定义成 Jigsaw(solo.c:471):行数滑到 1 棋盘会悄悄变成 jigsaw、勾选框自己亮起;而勾掉 Jigsaw 提交回去的仍是 r = 1,勾不掉。列数相应封到 order/2。见「与上游的出入」。',
      '二阶配 4 向旋转、4 向镜像、8 向镜像(2j 与 2×2 都是),以及二阶 Killer(2jk),上游生成不终止:原生 oracle 五个种子全部超时。表里去掉,见「与上游的出入」。',
    ],
  },
  {
    id: 'mines', title: 'Mines', file: 'mines.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽',
        u: ['full 且「Ensure solubility」时宽高都 > 2(290)', '否则宽高 ≥ 1(292)', '≤ SHRT_MAX(294)', '面积 ≤ 2²⁸−1(300)'],
        f: '4..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上', '雷数 ≥ 1 且 ≤ 面积−9 ⇒ 面积 ≥ 10'],
        f: '4..100', d: '—', c: 'cap' },
      { label: 'Mines', kind: 'int', sem: '雷数(绝对个数);−9 是给第一次点击留一块 3×3 的安全区',
        u: ['≥ 1(305-308)', '≤ 宽×高 − 9(309)'], f: '1..宽×高−9', d: '看宽高;缩小棋盘时被夹', c: 'up' },
    ],
    notes: [
      '**定下来的**:宽高 4..100、雷数 1..面积−9 全放开,读数是绝对个数(占比作为旁边的小字)。'
        + '4 是 owner 定的下限,顺带解决两件事:3×3 的面积只有 9,连一颗雷都放不下(雷数 ≤ 面积−9);'
        + '而 3×n 一到高密度,唯一解那条路修不出来(3×100 撒 30% 的雷跑五分钟不出结果)。4 起面积恒 ≥ 16,'
        + '高也不用再看宽,开关也不再影响下限。',
      '**耗时不落在「新游戏」上,落在第一次点击**:interactive 模式下 `new_game_desc` 只把随机种子编码进 desc(2065-2072),'
        + '真正的布雷发生在点开第一格的时候(那一格决定安全区)。四十个游戏里只有这一个是这样。',
      '**成本 100% 来自「Ensure solubility」**:`minegen` 里勾着才走求解器 + 扰动 + 重开那条路,不勾就是随机撒雷一轮结束(1968)。'
        + '实测同一组参数:100×100 撒 3500 / 4000 颗雷,勾着 >30 秒,关掉 **0.000 秒**;70×70、50×50 同样。',
      '**耗时是一条驼峰,峰在 40%**(不是密度越高越慢)。细扫:50×50 在 30% 是 0.059 秒、34% 4.19、38% 9.60、'
        + '**40% 10.65**、44% 8.80、48% 3.96、54% 1.28;30×30 的峰也在 40%(1.56 秒)。满雷(面积−9)反而是瞬间的。'
        + '按棋盘看:30×30 整条都安全(峰值 1.6 秒),50×50 峰值 10.6 秒,70×70 与 100×100 的 34–48% 段 >30 秒。',
      '驼峰的成因在代码里读得出来,三条叠加:① 廉价推理只在「集合被填满或清空」时命中(`s->mines == 0 || == bitcount16(s->mask)`,846),'
        + '一个有 k 个未知格的集合被一击判掉的概率是 `(1−p)^k + p^k`,k = 8 时 p = 0.2 或 0.8 都是 16.8%、p = 0.5 只有 0.78%,**中间最难**;'
        + '② 开局的展开是低密度独有的红利——一格是 0 的概率 `(1−p)⁹`,10% 时 38.7%(点一下开半个盘)、40% 时只剩 1.0%,'
        + '**这条只帮低密度**;③ 最强的全局雷数推理(937)有两级,`minesleft == 0 || == squaresleft`(1003)只在极端密度附近成立,'
        + '而穷举所有集合不相交并那条被上游自己封在 `nsets <= lenof(setused)`、`setused[10]`(1026)——**集合超过 10 个就整条跳过**,'
        + '大盘中密度时前沿很长、集合远多于 10,于是这条最强的推理根本不执行,高密度时反而跑得起来。②③ 都不对称,'
        + '所以峰落在 40% 而不是 50%。推理断了就随机挑一个集合让 `mineperturb` 填满或清空(1220 附近),改不动就整盘重来'
        + '(`do…while(!success)`,1865/1970,没有次数上限;试满 100 次后才允许大改,1946)。',
      '**100×100 配 30–50% 的雷是明知的代价**:本机 >30 秒,owner 的机器十几秒起,而且卡在**第一次点击**上。'
        + 'owner 看过上面这张表之后仍选择上限保持 100、雷数不设上限。要收的话最省事的一条是「长边 ≤ 50 时不限,长边 > 50 时雷数 ≤ 25% 面积」——'
        + '50×50 的峰值(10.6 秒)正好卡在预算上,所以「长边封 50」等价于「雷数完全不用管」。',
      '最小棋盘:勾着唯一解时是 **4×4**(本仓库的下限);上游自己允许到 3×4(面积 ≥ 10、两维 > 2),不勾时到 1×10 / 2×5。'
        + '3×3 上游也拒——面积 9 时雷数上限成了 0,和「≥ 1」冲突。',
      '百分比写法随文本框一起下线:滑块只出绝对数,旁边附「占比」读数;同样的雷数都能达到。'
        + '上游那条 `custom_params`(266)只做换算不做钳位:9×9 输 `100%` 会被算成 81 颗然后被 `validate_params` 拒掉,'
        + '不会自动降到上限 72;而 `90%` 因为整数除法截尾正好算成 72,反而放行。滑块没有这个坑。',
    ],
  },
  {
    id: 'samegame', title: 'Same Game', file: 'samegame.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 1(291)', '宽×高不得溢出 int(293)'],
        f: '2..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高',
        u: ['「Ensure solubility」时面积 ≥ 2(302)', '否则每种颜色至少两格:面积 ≥ 2×颜色数,颜色 ≥ 2 ⇒ 面积 ≥ 4(305-310)'],
        f: '2..100', d: '—', c: 'cap' },
      { label: 'No. of colours', kind: 'int', sem: '颜色数(勾着时只是调色板上限,不保证每种都出现)',
        u: ['≤ 9(296)', '保证可解时 ≥ 3(300)', '不保证时 ≥ 2 且 2×颜色数 ≤ 面积(305-310)'],
        f: '2..min(9, ⌊面积/2⌋);勾「Ensure solubility」时下限 3、宽 > 20 时下限 4(2×2 只剩 3 那一档)',
        d: '看宽高和「Ensure solubility」', c: 'local' },
    ],
    notes: [
      '**定下来的**:宽高 2..100;色数上限统一封到 ⌊面积/2⌋;勾着「Ensure solubility」且宽 > 20 时色数下限抬到 4。'
        + '「Scoring system」和「Ensure solubility」两个开关都留着,一个都不删。',
      '**宽高从 2 起**不是避崩:上游只查面积,1×n 能生成也不崩、还是瞬时。理由是机制退化——消完的复位是'
        + '「先各列下落、再把空列整体左移」(`sg_snuggle`,1214),宽 1 永远没有列可移、高 1 永远没有格可落,'
        + '两种都塌成同一个一维消除游戏。两维都 ≥ 2 之后面积恒 ≥ 4,不勾时那条「面积 ≥ 2×色数 ⇒ ≥ 4」自动满足,'
        + '高的表也不用再看宽和开关了。',
      '**色数上限 ⌊面积/2⌋ 在勾着的时候同样成立,而且可证**:保证可解的生成器每次插两格(奇数面积开头有一次三格,24-26),'
        + '所以整盘的块数恰好 ⌊面积/2⌋,能出现的颜色不可能比块多。实测在小盘上正好取等,一格不差(9 色、每格 3 个种子,'
        + '数 desc 里的不同值):1×2 用 1 色、2×2 用 2、3×3 用 4、4×4 用 7、6×6 用 8–9、10×10 用 9。'
        + '也就是说上游放行的那些高色数档在小盘上是死档,拖过去生成的还是同一种局面。',
      '**只有「三色 + 保证可解」会爆,其余整个 100×100 都是免费的。** 堵色那句是 `if (nwrong == nc) continue`(528),'
        + '`nwrong` 是去重后的正交邻居颜色数,一格最多 4 个邻居 ⇒ **五色及以上永远堵不住,这是鸽笼不是统计**。'
        + '实测:五色沿宽 = 100 那条边扫高 1..100,重来次数 ≤ 0.2;四色同样扫法 ≤ 8.5,整盒最坏出现在 100×100,'
        + '0.21 秒(浏览器实测 0.17)。堵色概率也能算出来:邻居颜色独立均匀时,三色要 4 个邻居覆盖 3 个值 = 36/81 = 0.444,'
        + '四色要 4 个邻居两两不同 = 24/256 = 0.094,五色起为 0(空格 0 也算一种「错色」,所以前沿更容易堵)。',
      '**三色的耗时有闭式**,两条律子相乘。① 重来是无记忆的:一轮里所有候选点都失败就 `n < 0` 整盘作废(837),'
        + '`while (!ok)` 从空盘重开(864),不带任何状态 ⇒ `E[重来] = 1/P − 1` 是恒等式,上游自带的 `COUNT_FAILURES` 正好把它打出来。'
        + '② 宽是唯一的指数轴,高不是:奇偶约束的条数 = 未满列的连续段数 ≤ **列数**(619-647 的扫描是 `for (i = 0; i < w; i++)`),'
        + '与高无关。于是 `ln(E[重来]+1) = a·宽 + b`——高 20 时 a = 0.552、b = −1.111、**R² = 0.99935**(12 个点,宽 4..22,'
        + 'E 从 2.0 跨到 68302);高 100 时 a = 0.511、b = −1.245、R² = 0.995。**每加一列 ×1.7**。'
        + '③ 单次尝试是 Θ(面积²)(每试一个候选点都 `memcpy` 整盘,438;轮数 Θ(面积)),实测 **1.22×10⁻⁹ × 面积² 秒**,'
        + '九个点(面积 2500..10000)误差 5% 以内,且只看面积(100×20 和 20×100 同值)。'
        + '合起来 `T = (1 + e^(a·宽+b)) × 1.22×10⁻⁹ × 面积²`,和独立测的墙钟对得上:高 100 时宽 12/14/15/16 预测 0.23/0.89/1.70/3.22,'
        + '实测 0.24/0.84/1.59/3.19,浏览器(wasm)0.37/0.81/1.14/5.93 —— wasm 和原生同量级。'
        + '四色的同一条律子 a 只有 0.022(高 10),差 25 倍,这就是三色和四色的分水岭。',
      '**宽 20 是 owner 定的,明知代价**:三色宽 20 的实测(每点 5 个种子,本机比 owner 的机器慢 2–3 倍)——'
        + '20×15 平均 3.4 秒、20×20 4.5、20×30 7.0(最坏 25.8)、20×40 15.8(最坏 37.4)、20×50 38.1(**最坏 111**)、'
        + '20×75 9.1、20×100 16.7。重来次数服从几何分布,尾巴很长,单局波动比这张表大。'
        + '要收的话按公式解出来的曲线是:高 ≤ 20 给到宽 20、高 50 给到 17、高 100 给到 16,那样任何组合都在 5 秒内;'
        + 'owner 选了平的 20,接受高盘慢。15 那个值另有一条好处:正好保住上游最大的三色预设 15×10c3。',
      '**可读性**:这个游戏没有数字也没有网格线,唯一的视觉分隔是块间那道缝,而缝宽 `tilegap = (格边长 + 8) / 16` 是整除(1360)——'
        + '**格边长 < 8 px 时缝宽变 0,块的边界直接不画**。实测每格像素:1440×900 上 30×30 是 24.7、50×50 是 14.3、'
        + '80×80 是 9.1、100×100 只有 **7.1(无缝)**;2560×1440 上 100×100 是 12.1(有缝);390×844 手机上 50×50 就 7.1 了。'
        + '按「大屏桌面为准」100 仍然过关,但笔记本屏上的 100×100 会掉到没有缝那一档;真要对得起小屏,临界点是 90。',
      '**「Ensure solubility」是两种玩法,不是一个生成质量开关**(所以留着)。勾着 = 谜题:目标是清空(`game_status` 只在 `complete` 时判胜,1638),'
        + '盘面碎;不勾 = 街机(原版 Chain Shot):`gen_grid_random`(893)一遍撒完,块大一倍。实测(每格 30 个种子,'
        + '先 400 次随机试走证「能清空」,再穷举证「不能」):10×10c3 平均最大块 5.4 对 11.0、贪心得分 168 对 472;'
        + '**五色起随机盘基本清不空**——5×5c5 只有 4/30、8×8c8 是 0/30(14 局证否,贪心打完平均还剩 30.7 / 64 格),'
        + '而保证可解那侧「证否」恒为 0。街机模式还是 2 色唯一的去处(勾着时上游硬性要求 ≥ 3)。',
      '**「Scoring system」只作用于记分**(所以也留着):`scoresub` 唯一的使用点是 `npoints`(129),只被 `execute_move`(1343)'
        + '和状态栏(1596)调用;两个生成器一次都没读过它。实测同种子换计分法,desc 逐字节相同。'
        + '区别只在最小的那一步:`(n-2)²` 下消两格得 0 分,而出题机器的解全程都是两格步 ⇒ **机器的解正好得 0 分**'
        + '(奇数面积得 1,那一次三格步),玩家的分数字面上就是「比机器强多少」——上游注释 33-37 就是这么说的,'
        + '实测限制只走两格步,5×5c3 十五个种子全部清空得 1 分、6×6c3 与 6×6c4 全部得 0 分。'
        + '`(n-1)²` 没有这个零点(机器的解就能白拿 面积÷2 分),而且**分辨力更差**:同一批盘面,'
        + '「200 次试走里最好的一条」比「随机走一条」拉开的倍数,`(n-2)²` 是 2.86/3.27/3.32/2.89(10×10 的 3/5/9 色、15×10c3),'
        + '`(n-1)²` 只有 2.33/2.07/1.70/2.38 —— 它给每一步都加底分,把好坏差距摊薄了。'
        + '顺带:每次都消最大的块得分反而更低(10×10c3 贪心 168、随机 237),这个分数是有本事含量的。',
    ],
  },
  {
    id: 'flip', title: 'Flip', file: 'flip.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽',
        u: ['宽高都 > 0(191)', '宽 ≤ (INT_MAX−3)/高(193)', '面积² ≤ INT_MAX−3 ⇒ 面积 ≤ 46340(195)'],
        f: '2..50', d: '主', c: 'local' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'],
        f: '2..min(50, ⌊1000/宽⌋):边长封 50,面积封 1000', d: '看宽', c: 'local' },
    ],
    notes: [
      '**定下来的**:宽高各 2..50,且面积 ≤ 1000,两种「Shape type」同一套。'
        + '上游自己的预设只有 3×3 / 4×4 / 5×5(两种模式各三个),它本来就不是给大棋盘用的。',
      '**这个游戏的成本是面积的平方,而且只看面积。** 矩阵是 `wh × wh` 字节(365),而 `new_game_desc` 把它'
        + '整个 hex 编进描述(596-600,两种模式都编),所以 **DESC 长度 = ⌈面积²/4⌉ + ⌈面积/4⌉ + 2** 个字符,'
        + '里面没有宽高单独出现的位置。实测面积 400 的七种形状(20×20 / 10×40 / 40×10 / 8×50 / 50×8 / 4×100 / 100×4)'
        + 'DESC 全是 40 102 字符,一字不差;Crosses 的耗时同理只看面积(面积 2500 的 50×50 / 25×100 / 100×25 是 0.018 / 0.017 / 0.018 秒)。',
      '**DESC 落在三个地方**:存档(`puzzles.save.flip`)、菜单里那行「游戏 ID」(直接渲进 DOM)、React state。'
        + '对比同样 50×50:Mines 的 DESC 是个随机种子(二十来字符)、Same Game 是每格一个颜色号(5 000 字符)、'
        + '**Flip 是 1 563 127 字符**。浏览器给一个站的 localStorage 约 5 MB 且全 app 共用,而 `writeSave` 是静默吞异常的——'
        + '超了不报错,只是这一局重开就没了。实测:面积 3200(64×50,256 万字符)写得进但正好吃光配额,'
        + '面积 3364(58×58)起 QuotaExceededError。菜单打开耗时:面积 900 是 76 ms、2500 是 398 ms、3600 是 1 228 ms、10000 是 **9 559 ms**。',
      '**Random 模式的生成耗时随面积指数涨**,Crosses 不涨。Crosses 是纯 Θ(面积²):浏览器里面积 1600 只要 57 ms、'
        + '面积 10000 也只要 1 291 ms。Random 是拒绝采样——加完格子后要求「没有两行完全相同」,否则整个重来,没有次数上限'
        + '(508-524,上游注释自己预测了这个 scalability 问题)。合起来 **T ≈ 4.3×10⁻⁷ × 面积² × e^(0.00345×面积) 秒**,'
        + '两个系数只用面积 400 和 900 两点定,其余全是留出来验证的,原生实测(多种子平均):'
        + '面积 100 → 0.012、200 → 0.031、400 → 0.22、600 → 0.95、800 → 4.8(最坏 11.8)、900 → 5.6(最坏 14.5)、'
        + '1000 → 14.8(最坏 42.5)、1200 → 50.9(最坏 150)、1400 → 93.5(最坏 271)、1600 → 231.8。公式在这些点上偏差 ≤ 1.7 倍。'
        + '同面积 1600 时 Crosses 是 57 ms,**差 4 万倍**。',
      '**形状只在极瘦时才有影响,幅度远小于面积项。** 面积 400 的实测(20 个种子):20×20 0.26、40×10 0.23、50×8 0.29、'
        + '80×5 0.45、100×4 0.79、200×2 >30 秒、**400×1 只要 0.004 秒**。机制:Random 要加 `4×面积 − 2(宽+高)` 个格子,'
        + '可加的候选有 `(3宽−2)(3高−2) − 面积` 个,两者一减正好是 **余量 = 4×(宽−1)×(高−1)**——这是生成器的自由度。'
        + '余量占「要加的量」的比例:20×20 是 95%、100×4 是 85%、200×2 是 66%、1×n 是 **0**。余量越薄越容易撞上重复行;'
        + '而短边 = 1 反过来最快,因为余量归零、矩阵被完全钉死,没得选也就没得拒。定下来的范围里最瘦的 40×2 只要 0.07 秒。',
      '**宽高从 2 起是避挂,不是调优。** 1×n 的余量正好是 0 ⇒ 矩阵被钉死,加出来必然就是十字矩阵'
        + '(实测 1×3 / 1×4 / 1×5 / 1×8 / 1×20 / 5×1 换种子都和 Crosses 逐字节相同,**1×n 的 Random 根本不随机**);'
        + '而 1×2 的十字矩阵两行相同,撞上「不许两行相同」⇒ **永远重来,死循环**。四个种子基各跑 15 秒都不出,与种子无关。'
        + '宽高 1..8 的 48 个组合全扫过,只有 1×2 和 2×1 会挂;抬到 2 之后又把宽高 2..12 的 121 种棋盘 × 两种模式 × 各 2 个种子'
        + '(242 组)跑了一遍,零出错。',
      '**上限 50 / 面积 1000 是 owner 定的,明知代价**:面积 1000 时 Random 平均 14.8 秒、最坏 42.5 秒,'
        + '浏览器里抽到过 59.7 秒(这个分布尾巴极长,均值和单局体感差很远)。我的建议是面积 800(平均 4.8 秒)。'
        + '存档那头面积 1000 只占 5 MB 配额的 10%,很宽裕——Crosses 单独看其实能到面积 1600–2500,owner 选了两种模式同一套。',
      '**格子里有信息,32 px 以下就糊了**:每格要画一张小图标出「按下去翻哪几格」(1152-1170),'
        + '小图的格边长 `td = TILE_SIZE / 16`(不足 1 取 1),**td = 1 时退化成一坨 2 px 方块**。td ≥ 2 需要格边长 ≥ 32 px:'
        + '2560×1440 上边长 ≤ 38、1440×900 上 ≤ 21、390×844 手机上 ≤ 11。Crosses 模式这张图是废话(永远是十字),'
        + 'Random 模式它是唯一能知道一次点击做什么的信息。边长封 50 没有卡在这条上——面积 1000 时方盘只有 31×31。',
    ],
  },
  {
    id: 'guess', title: 'Guess', file: 'guess.c', tuned: true,
    params: [
      { label: 'Colours', kind: 'int', sem: '可选颜色数', u: ['≥ 2(219)', '≤ 10(223)'], f: '2..10', d: '主', c: 'up' },
      { label: 'Pegs per guess', kind: 'int', sem: '每次猜的钉数',
        u: ['≥ 2(219)', '不允许重复时 ≤ 颜色数(227)', '允许重复时无上限'],
        f: '允许重复:2..50;否则 2..颜色数', d: '看颜色数和「Allow duplicates」;颜色数减少时被夹', c: 'local' },
      { label: 'Guesses', kind: 'int', sem: '允许猜的次数', u: ['≥ 1(225)', '无上限'], f: '1..50', d: '—', c: 'local' },
    ],
    notes: [
      '**定下来的**:颜色 2..10(上游原样)、钉数 2..50(不勾「Allow duplicates」时 2..颜色数)、次数 1..50。'
        + '两个开关都留着,它们是规则变体不是技术开关。',
      '**这个游戏没有生成成本,也没有任何危险组合。** `new_game_desc` 是 O(钉数) 一遍过;唯一的重挑发生在'
        + '不勾「Allow duplicates」时(`goto newcol`),而那时上游保证颜色数 ≥ 钉数(227),期望次数是 n·H(n)——'
        + '10 钉也才 29 次。真机极端组合 **10 色 / 100 钉 / 100 次:落定加画完 614 ms,零报错**;'
        + '最小 2 色 / 2 钉 / 1 次同样正常。所以上限完全是可读性和游戏性的事。',
      '**布局是一条线性式子**(1029-1053,常数在 1025-1027:BORDER 0.5、PEG_GAP 0.10、PEG_HINT 0.35):'
        + '`画布宽 = 钉边长 × (2.9 + 1.1×钉数 + 0.45×⌊(钉数+1)/2⌋)`、'
        + '`画布高 = 钉边长 × max(0.9 + 1.1×颜色数, 2.1 + 1.1×次数)`。两个轴分开:宽只看钉数,高只看'
        + '「颜色数和次数的较大者」。钉子里那个数字用 `PEGRAD` 当字号 = **钉边长的一半**(1254),'
        + '右边的提示点是 0.35 倍。1440×900 实测(画布约 1412×700):20 钉 48 px、30 钉 33、50 钉 20、100 钉 10;'
        + '次数 10 是 53 px、20 是 29、30 是 20、50 是 12、100 是 6。手机 390×844 上 10 钉 24 px、15 钉 16 px。',
      '**要几次才猜得出来:99 个格子实测。** 两套独立解法互相对过表——空间 ≤ 130 万时枚举全部密码跑'
        + '「随机一致」和「最小最大(Knuth)」两种策略(6 色 4 钉上强玩法平均 4.5 / 最坏 6,和文献已知最优'
        + '平均 4.476 / 最坏 5 对得上);空间更大时用局部搜索找一个与全部历史反馈相符的密码来猜,'
        + '在 6 色 4 钉和 10 色 6 钉上与枚举法一致。**「随机一致」的平均次数**(色数 × 钉数):'
        + '2 色:钉 4 → 2.9、10 → 5.4、20 → 9.1、**50 → 17.3**;6 色:4 → 4.7、10 → 8.0、20 → 14.3;'
        + '10 色:4 → 6.1、10 → 9.9、20 → **16.0**。不勾「Allow duplicates」时 10 色 10 钉是 11.0(最坏 14)。',
      '**次数的公式**(99 个实测点,R² = 0.978,最大偏差 2.0 次):'
        + '`平均次数 ≈ 0.1 + 0.96×log₂(颜色数) + 钉数 × (0.24 + 0.12×log₂(颜色数))`——'
        + '每多一个钉子加 (0.24 + 0.12·log₂色) 次,2 色时每钉 0.36、10 色时每钉 0.65。'
        + '外推到 50 钉验证过一次:2 色 50 钉公式 19.3、**实测 17.3**(最坏 18)。'
        + '按它算 10 色 50 钉要 **36 次**、6 色 50 钉 31 次。'
        + '顺带:信息下界(⌈钉×log₂色 / log₂((钉+1)(钉+2)/2−1)⌉)完全不能当参考——它说 2 色 20 钉只要 3 次,'
        + '实测 9 次;10 色 20 钉它说 9 次,实测 16 次。反馈分布远不均匀,那个上界拿不到。',
      '**上游预设给的次数约是平均值的 2 倍**:6 色 4 钉平均 4.7、上游给 10;8 色 5 钉平均 5.9、上游给 12。'
        + '按这个手感,10 色 20 钉要 32 次、10 色 50 钉要 70 次。',
      '**50 / 50 是 owner 定的,明知代价**。可解性上没问题:10 色 50 钉平均 36 次,50 次给到 1.4 倍富余,'
        + '而且钉数越多分布越窄(平均与十来局里最坏的差:6 色 20 钉 1.7、10 色 20 钉 1.0、2 色 50 钉 0.7),'
        + '最坏大概落在 40 上下。代价在可读性:**50 钉 × 50 次时钉边长只有 12.3 px、数字 6.1 px、提示点 4.3 px**,'
        + '而提示点是这个游戏唯一的信息来源(右边是 25 列 × 2 行的点阵)。另外一局要点约 1800 下'
        + '(36 手 × 50 个钉子)。我的建议是钉 20 / 次数 30(钉边长 20 px、提示点 7 px,最难的角落仍有 1.9 倍富余)。',
    ],
  },
  {
    id: 'pegs', title: 'Pegs', file: 'pegs.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽',
        u: ['full 时宽高都 > 3(192)', 'Cross 只允许 5×7、5×9、7×5、7×7、7×9、9×5、9×7、9×9(206-217)', 'Octagon 只允许 7×7(225-228)', 'Random 无额外限制'],
        f: 'Cross:{5, 7, 9};Octagon:{7};Random:4..30', d: '看「Board type」', c: 'local' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'],
        f: 'Cross:{5, 7, 9},宽 5 时 {7, 9};Octagon:{7};Random:4..30', d: '看类型和宽', c: 'local' },
    ],
    notes: [
      '**定下来的**:Cross 和 Octagon 照抄上游的穷举,Random 宽高各 **4..30**(下限 4 就是上游自己的 > 3)。'
        + 'Cross 和 Octagon 的表只有几个值,滑块的每一档都是合法尺寸;Octagon 两格都钉死,控件禁用。',
      '**上游那两条穷举是可解性结论,不是偷懒**:Cross 只放行八种对称尺寸,注释里引的是 George I. Bell 关于广义十字盘的可解性列表;'
        + 'Octagon 只放行 7×7,而且文件里附了一整页染色反证,说明中心开孔的欧洲八角盘**本身无解**(把盘按三色斜条染色,'
        + '任何一步都让三个计数的奇偶同时翻转),所以上游把起始孔挪开了。这两种盘是按公式直接铺出来的,一次成型,没有任何生成成本。',
      '**Random 是拒绝采样,而且拒绝的理由是几何的。** `pegs_generate` 从中心一颗棋子反向走子长出一片,'
        + '然后要求这片**四条边都够到**(`extremes == 15`),否则整盘重来,**没有次数上限**。把 `pegs_genmoves` 单独抠出来跑'
        + '(把 pegs.c 整个 include 进一个探针,不动 vendor 一行)量出三件事:'
        + '① 那一片的**面积恒是盘面的 57%**——`nmoves < 面积/2` 之后就不再接受代价 2 的走子(1148),'
        + '实测从 10×10 到 100×100、从方盘到细条全是 57%;② 它是**团状**的(生成器优先走代价 0 的子,紧凑铺开,不长细胳膊);'
        + '③ 于是细长盘上它很快把短边顶满,之后只能横着长成带子,带宽 ≈ 0.57×面积 ÷ (0.87×短边) = **0.66×长边**——**永远够不到两头**。'
        + '包围盒实测(每格 200 次):100×100 是 94.9×95.4(长边 95%);20×10 是 17.4×**10.0**(87%);30×10 是 24.1×10.0(80%);'
        + '100×10 是 66.2×**10.0**(**66%**);100×50 是 77.4×50.0(77%)。短边那一列永远顶满,长边永远差一截。',
      '**通过率**(两万次尝试)。方盘:4×4 86%、10×10 36%、20×20 24%、30×30 15%、50×50 6.0%、100×100 1.0%——'
        + '大约按 面积^−0.78 掉,所以 100×100 平均要重开一百次、耗时 2.87 秒(浏览器 1.39 秒)。'
        + '细长盘直接掉到零:**50×4 到 50×15 全是 0/20000**;能量到的最差有限值是 50×20(通过率 1/10000,'
        + '真生成器实测平均 29.7 秒、最坏 54.9 秒),50×25 是 4.62 秒,50×30 才回到 0.24 秒。'
        + '这一整片今天在 4..100 的表里都点得到:100×10、10×100、100×50、50×100、100×4、4×100 各跑 300 秒,一个都没出来。',
      '**上限取 30 之后不再需要额外的长宽比规则**,因为 30 以内最瘦的组合还撑得住:真生成器实测(每格 20 个种子)'
        + '30×4 平均 1.60 秒最坏 7.37、4×30 平均 1.89 最坏 5.96、30×5 平均 1.81 最坏 9.50、30×6 平均 1.60、30×8 平均 0.61、'
        + '30×10 平均 0.31、30×30 平均 0.015。要再收的话「|宽−高| ≤ 20」会把最坏从 1.8 秒压到 0.44 秒,但没有必要。'
        + '(如果哪天上限抬回 50,那条长宽比规则就是必需的——50×15 及更瘦的组合根本打不开。)',
      '**要走多少步 = 面积 ÷ 2。** 反向每走一步净加一颗棋子(拿掉 1 颗、放上 2 颗),而生成停在 面积/2 步附近,'
        + '所以棋子数 ≈ 面积/2 + 1;胜利条件是「场上只剩一颗」(1039-1045),于是步数 = 棋子数 − 1。'
        + '真机实测:7×7 Cross 32 颗 / **31 步**(这就是经典孔明棋)、10×10 51 颗、20×20 202 颗 / 201 步、'
        + '30×30 453 颗 / **452 步**、50×50 1252 颗、100×100 5001 颗 / **5000 步**。上游自己的 Random 预设最大只到 9×9。'
        + '30 这个上限对应 452 步,已经是经典孔明棋的 15 倍。',
      '**可读性**:棋盘 = 一格 × (宽+1) × (高+1)(1069),棋子是纯色圆点、没有文字,但操作是「先点一颗棋子,'
        + '再点两格之外的落点」,对点击精度的要求比「点一片」高。1440×900 真机:10×10 是 64.9 px、20×20 是 35.0、'
        + '**30×30 是 24.0**、50×50 是 14.0、100×100 是 7.0。',
    ],
  },
  {
    id: 'dominosa', title: 'Dominosa', file: 'dominosa.c', tuned: true,
    params: [
      { label: 'Maximum number on dominoes', kind: 'int', sem: '骨牌上的最大点数 n;棋盘 (n+2)×(n+1) 格,骨牌 (n+1)(n+2)/2 张',
        u: ['≥ 1(249)', '无上限(仅防溢出,251)'],
        f: '1..上限,上限随难度:Trivial 25 / Basic 30 / Hard 15 / Extreme 10 / Ambiguous 50',
        d: '看「Difficulty」', c: 'local' },
    ],
    notes: [
      '**定下来的**:n 的上限随难度走——Trivial 25、Basic 30、Hard 15、Extreme 10、Ambiguous 50,下限五档都是上游的 1。'
        + '上游对 n 只有「≥ 1」和防溢出两条,没有实质上限。',
      '**成本按难度裂开,而且是 U 形的**,机制在生成循环的两句判据(2337-2345):'
        + '`run_solver(sc, diff) > 1` 就重来(这个难度解不出来),`sc->max_diff_used < diff` 也重来(更低的难度就能解出来)——'
        + '要求局面**恰好**需要这个难度,而且没有次数上限。Hard 以上还要先过两道预筛(2317-2334):`alloc_try_hard`,'
        + '再要求 Basic 解不出来、且没有任何骨牌只剩一个可放位置。Ambiguous 那一支根本不跑求解器(2289)。'
        + '于是 **Basic 最便宜**(大多数随机盘天然落在这一档),**Trivial 贵**(盘子一大,「只用最傻的推理就能解完」越来越罕见),'
        + '**Hard / Extreme 最贵**(要求基本推理做不出来),**Ambiguous 免费**。',
      '**实测**(原生,每格 3–5 个种子)。Trivial:n=15 → 0.117 秒、18 → 0.534、**20 → 2.35**、21 → 3.20、22 → 10.8、23 → 18.3、25 → 17.1。'
        + 'Basic:15 → 0.134、18 → 0.439、20 → 0.761、**25 → 5.25**、26 → 15.4、27 → **59.9**、28 → >180。'
        + 'Hard:6 → 0.065、8 → 1.20、9 → 2.10、**10 → 1.64**、11 → 25.9(单局)、12 起五个种子全部 >120 秒,一直到 98。'
        + 'Extreme:6 → 0.223、7 → 0.317、**8 → 1.71**、9 → 11.7、10 → 10.9、11 → 24.9、12 → 单局 900 秒不出,12 起同样整段打不开。'
        + 'Ambiguous:n=98(100×99 的盘)也只要 0.009 秒。**小 n 那头没有坑**:n=1..5 在五个难度下全部瞬时'
        + '(专门查过「3×2 的盘能不能要求 Extreme 推理」,实测能)。',
      '**上限是 owner 定的,明知代价**:选的 25 / 30 / 15 / 10 / 50 都比我建议的 20 / 25 / 10 / 8 / 30 高一档。'
        + '这四档顶格的单种子实测:**Trivial 25 → 1.7 秒、Extreme 10 → 8.1 秒(浏览器里抽到过 35 秒)、'
        + 'Basic 30 → 92.3 秒、Hard 15 → 单局 900 秒也没出来**。也就是说 Hard 那一档的顶格不是「慢」,'
        + '是点下去就再也回不来。要收回来的话,我建议的那一组对应的实测分别是 2.35 / 5.25 / 1.64 / 1.71 / 0.000 秒。',
      '**可读性**:棋盘 (n+2)×(n+1),每格写一个 0..n 的数字,字号 = 格边长的一半(3257),n ≥ 10 起是两位数。'
        + '1440×900 实测字号:n=9 是 32 px、15 是 21、20 是 16.5、25 是 13.5、30 是 **11.5**、40 是 8.5、50 是 7.0、98 是 3.5;'
        + '2560×1440 上 50 是 12、60 是 10;390×844 手机上 15 就只有 10.5 了。按大屏字号 ≥ 10 px 是 n ≤ 60,'
        + '按笔记本 ≥ 11.5 px 是 n ≤ 30。',
      '**工作量** = 要摆的骨牌数 = (n+1)(n+2)/2:n=9 是 55 张(上游最大的预设)、20 是 231、25 是 351、30 是 496、50 是 1326。',
      '`Ambiguous` 这一档就是上游删掉的那个「确保唯一解」开关——`decode_params` 里 `\'a\'` 那一支的注释写着'
        + ' "Legacy encoding from before the difficulty system"。上游把 checkbox 折进了难度梯,12 个预设里没有一个用它。',
    ],
  },
  {
    id: 'untangle', title: 'Untangle', file: 'untangle.c', tuned: true,
    params: [
      { label: 'Number of points', kind: 'int', sem: '点数', u: ['≥ 4(224)', '无上限(仅防溢出,230)'], f: '4..100', d: '—', c: 'cap' },
    ],
    notes: [
      '**定下来的**:4..100,一格没改——下限就是上游自己的,上限 100 由 owner 真机验过(生成不慢、也看得清)。'
        + '唯一的 string 控件,没有跨参数依赖,所以也没有夹值问题。',
      '**下限 4 要看对分支**:`validate_params`(222-232)有两套门槛,`#ifndef EDITOR` 那支是 **n ≥ 4**、'
        + '`#else` 那支是 n ≥ 1。`EDITOR` 只在 `CMakeLists.txt:272` 的 `grapheditor` 那个独立程序上定义'
        + '(`guiprogram(grapheditor untangle.c ... COMPILE_DEFINITIONS EDITOR)`),`puzzle(untangle ...)` 这条正常目标不带它——'
        + '我们的 wasm 走的是后者,所以生效的是 **n ≥ 4**,那条 n ≥ 1 够不着。',
      '上游那条 `n ≤ INT_MAX/3`(230)只是防溢出,离 100 差着十个数量级。预设 6 / 10 / 15 / 20 / 25,默认 10,都落在表里。',
    ],
  },
  {
    id: 'blackbox', title: 'Black Box', file: 'blackbox.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 2(193)', '≤ 255(197)'], f: '2..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '2..100', d: '主', c: 'cap' },
      { label: 'No. of balls', kind: 'span', sem: '球数;写「3」是固定数,写「3-6」是区间,每局在区间里随机取(224-225),玩家只知道区间',
        u: ['sscanf "%d-%d",不成对就当单个数(184-186)', '最少 ≥ 1(199-202)', '最少 ≤ 最多(203)', '最少 < 宽×高(205)', '最多上游没查:超过格数时放球会死循环(239-242)'],
        f: '最少 1..宽×高−1;最多 最少..宽×高−1', d: '看宽高;缩小棋盘时被夹', c: 'sem' },
    ],
    notes: [
      '**定下来的**:2..100 与 1..宽×高−1,一格没改。owner 看过下面的实测后决定**暂不做游戏性优化**,'
        + '先按上游自己的线走;球数那条「好玩」的上限记在最后一条,留给以后。',
      '**控件**做成一对滑块(最少 / 最多),两者相等就写回单个数,和上游回显一致。「最多」的上限是本仓库按语义补的:'
        + '放球是拒绝采样(`do { 随机取一格 } while (那格已有球)`,239-242),没有次数上限,球数超过格数就永不终止;'
        + '上游只对「最少」查了这条(205),`8×8` 配 `1-100000` 它照样放行。'
        + '顶到 `宽×高−1` 的期望尝试次数是 `面积·(H_面积 − H_2)`,一万格约 8.3 万次,可以忽略。',
      '**激光不会绕圈**:`fire_laser_internal` 是 `while(1)`,而且转向是原地转不移动,理论上可能有环'
        + '(转移映射不是单射:同一个 (格, 朝向) 能从「走一步进来」和「原地转过来」两条路到达)。'
        + '把 `blackbox.c` 整个 include 进探针复刻走法(和上游 `laser_exit` 交叉校验 600 局全部射击口零处不一致),'
        + '按「步数 > 4×面积 = 状态数就必然在环里」判定,穷举了 2×2 / 3×3 / 4×4 / 5×4 / 6×3 / 6×4 / 5×5 / 7×4 / '
        + '8×3 / 9×3 / 12×2 / 6×5 的**全部 15 亿个布局**,再对 10×10 / 20×20 / 50×50 / 100×100 / 100×2 / '
        + '100×10 / 30×8 各十档密度抽样 14 万局:一次都没有。',
      '**耗时和存档都不是约束**:生成只是随机撒球,`w100h100m9999M9999` 三个种子平均 0.008 秒,'
        + '连上游自己的顶格 `w255h255m65024M65024` 也才 0.066 秒。DESC 是 `4n+4` 个十六进制字符(252),'
        + '100×100 塞满 9999 球是 4 万字符,比 Flip 已经接受的 25 万小一个量级。',
      '**可读性是 100 的唯一代价**:画布 `(宽+3)×(高+3)` 格(1159-1160),边框格里写激光编号(最多到 宽+高,三位数),'
        + '字号写死成格边长的一半(1383)。2560×1440 实测字号:30 是 19.5 px、50 是 12、60 是 10、70 是 8.5、'
        + '**75 正好 8.0**、80 是 7.5、100 只有 6.0;1440×900 上这条线落在 42。'
        + '按 Fifteen / Sixteen 那条「大屏字号 ≥ 8 px」的口径应当封 75,owner 选择保留 100。'
        + '另:格边长 ≤ 6 px 时上游每格画的 `crad−3`(1167、1282-1285)为负,canvas 的 `arc` 遇负半径必抛,'
        + '整块棋盘会变成纯黑——已在 renderer 里把半径夹到 0 修掉,那不是参数能挡住的(手机 55×55 起就到)。',
      '**球数「好玩」的上限(记着,没用上)**:信息只能从 `2(宽+高)` 个射击口进出。一条激光平均能走 `面积/球数` 格'
        + '(`E[步数] = (1−p)³/p`,500×500 上验过,密度越高越准),所以照到的格数 ≈ `2(宽+高) × 面积/球数`,'
        + '要盖住整块盘就得 `球数 ≲ 常数 × (宽+高)`——**跟面积无关,只跟一圈边有多长有关**。'
        + '实测「一个球都不落在暗格(全程没被任何激光看过的格子)」的最大球数:5×5 是 7、8×8 是 11、10×10 是 14、'
        + '20×20 是 27、50×50 是 68、100×100 是 134,除以 (宽+高) 稳定在 0.67–0.70(细长盘 0.48–0.73)。'
        + '暗格里的球对观测贡献严格为零(把它挪到另一个暗格,全部激光结果一字不变),而上游只要求「能重现全部激光」就算赢'
        + '(705),所以超线之后那部分球是填空不是推理。上游自己的五个预设全落在 `球数/(宽+高) ≤ 0.5`,'
        + '最大的 `10×10/4-10` 正好压线。要收的话 `⌊(宽+高)/2⌋` 是实测线的 0.75 倍,恰好装下全部预设。',
    ],
  },
  {
    id: 'slant', title: 'Slant', file: 'slant.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽(格数;线索画在格点上,线索阵是 (宽+1)×(高+1))',
        u: ['宽高都 ≥ 2(239-240)', '宽 ≤ INT_MAX/高(241-242,纯防溢出)'], f: '2..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上,一条判两个'], f: '2..100', d: '—', c: 'cap' },
    ],
    notes: [
      '**定下来的**:2..100,一格没改。owner 看过下面的耗时后决定**暂不设上限**——发牌已经挪进镜像 worker,'
        + '顶格是「转圈等几分钟、可取消」,不再是页面变砖。要收的时候按面积收,候选写在最后一条。',
      '**下限 2 是上游自己说清楚的**(230-237 的注释):生成器连 0 都扛得住不崩,只是零面积的局一开始就解完了;'
        + '而**边长为 1 的局做不出 Hard**,所以上游「最省事的办法是一律禁掉」。Difficulty 是 choices(Easy / Hard),'
        + '不用申报表,但模型读得到它——要按难度分上限的话有现成的入口。',
      '**造满格盘永不回溯**,上游给了证明(1074-1093):要出现「两种斜线都放不下」的格,得有两条已有路径分别连着这格的'
        + '两组对角、并且相交;但把格点按棋盘染色,任何连续的对角路径整条同色,而这两条路径的端点异色,不可能共点。'
        + '所以这一步是 Θ(面积·α)。',
      '**贵的是逐个试删线索**:填满全部 (宽+1)(高+1) 个线索,洗牌后走两趟,每个点删掉、跑一次求解器、解不出唯一解就放回去。'
        + '把 `slant.c` include 进探针复刻这段循环(复刻耗时和真生成器对得上:20×20 Easy 0.034 vs 0.038、'
        + '30×30 Hard 0.995 vs 1.026),数出来 **Easy 的求解器调用次数恰好是 `(宽+1)(高+1) + 1`**——20×20 是 442、'
        + '30×30 是 962,和公式一字不差(Easy 时每个点在第 0 趟就处理完,第 1 趟一个都不进)。'
        + 'Hard 时被删掉的点在第 1 趟会被再看一遍(值变成 −1 就落进 pass 1),多出一成左右。',
      '**外层的「恰好是这个难度」只在小盘上要重来,而且不会不终止**:Hard 要求「Easy 解不出来」,否则整盘重造。'
        + '200 个种子实测的平均轮数:2×2 是 1.77(最多 11)、3×3 是 1.38、5×5 是 1.04、**8×8 及以上恒为 1**。'
        + '另对 2×2 / 3×3 / 2×100 / 100×2 各跑 2000 个种子,全部瞬间返回。',
      '**耗时是边长的五次方多一点**。拟合(用 50 与 100 两点定,回代验证,40..100 之间误差 ≤ 16%):'
        + '`T_easy ≈ 208 × (边长/100)^5.69` 秒、`T_hard ≈ 324 × (边长/100)^5.13` 秒。本机单种子实测(秒)——'
        + 'Easy:20 = 0.04、30 = 0.31、40 = 1.18、50 = 4.02、60 = 12.5、70 = 30.6、75 = 38.5、80 = 60.9、90 = 108、**100 = 208**;'
        + 'Hard:20 = 0.13、30 = 1.03、40 = 3.45、50 = 9.26、60 = 24.5、70 = 56.3、75 = 70.2、80 = 123、**100 = 324**。'
        + '**边长翻倍,耗时乘 35–52 倍**;结构上说得通:调用次数 ∝ 面积,每次调用的成本自己也随面积长'
        + '(每次 solve 从 20×20 的 77 µs 涨到 60×60 的 1676 µs),两个一乘就是面积的 2.6–2.9 次方。',
      '**形状几乎不影响,只看面积**:面积都约 5600 的四种形状,Easy 是 75×75 = 38.5、100×56 = 36.9、56×100 = 30.0、'
        + '90×62 = 37.1 秒,Hard 是 75×75 = 70.2、100×56 = 65.8 秒——方盘略微最贵,差别 1.3 倍以内。'
        + '所以要收就按面积收,别按边长:100×10 只要 0.32 秒,不该被误伤。',
      '**可读性**:线索是画在格点上的单个数字 0..4(永远一位),字号 = 格边长的一半(1762),外面套半径 = 格边长 1/3 的圈;'
        + '画布 (边长+2) 格(BORDER = 一整格,1760)。实测字号:2560×1440 上 60 是 10.0 px、70 是 8.6 px;'
        + '1440×900 上 30 是 10.5、**40 正好 8.0**、50 是 6.5;390×844 手机上 20 是 8.5、30 是 6.0。'
        + '按「大屏字号 ≥ 8 px」那条线是 **75**。',
      '**要收的话(记着,没用上)**:`面积 ≤ 5625`(= 75 见方顶格)同时满足两条互不相干的判据——大屏可读性的 75,'
        + '以及顶格 Hard 70 秒(owner 的机器约 23 秒)。更保守的一档是 `面积 ≤ 3600`(60 见方,Hard 24.5 秒)。'
        + 'Hard 比 Easy 贵约 1.8 倍,也可以像 Dominosa 那样按难度分(Easy ≤ 6800、Hard ≤ 5625),只换来 Easy 多七格边长。',
    ],
  },
  {
    id: 'lightup', title: 'Light Up', file: 'lightup.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽',
        u: ['宽高都 ≥ 2(357-358)', '宽 ≤ INT_MAX/高(359-360,防溢出)', 'full:4-way rotational 只能方盘(364-367)', 'full:4-way(镜或旋)时不能宽高都 < 3——只禁 2×2(368-369)'],
        f: '下限按对称 × 难度查表(None 2/3/4、2-way 2/4/5、4-way 3/4/5,依次 Easy/Tricky/Hard);上限 100', d: '看「Difficulty」和「Symmetry」', c: 'local' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上,一条判两个'],
        f: '同宽;「4-way rotational」时钉在宽', d: '看难度、对称和宽;宽动时跟着动', c: 'local' },
      { label: '%age of black squares', kind: 'int', sem: '黑格占比', u: ['full:5..100(362-363)'], f: '5..90', d: '—', c: 'local' },
    ],
    notes: [
      '**定下来的**:宽高下限按「对称 × 难度」查表——None 2/3/4、2-way(镜和旋)2/4/5、'
        + '4-way(镜和旋)3/4/5,依次 Easy/Tricky/Hard;上限 100;黑格比例 5..90。'
        + '对称下标(96):0 None、1 2-way mirror、2 2-way rotational、3 4-way mirror、4 4-way rotational;'
        + '难度 0 Easy、1 Tricky、2 Hard,两个都是 choices,不用申报表但可以被宽高的表读到。'
        + '下限抬上来之后上游那两条边角(4-way 只禁 2×2、4-way mirror 宽 2 时高从 3 起)永远触发不了,'
        + '申报里不再重复写;只剩「4-way rotational 钉成方盘」还要自己守。',
      '**上游生成器会回不来,机制在 1558-1611**:`while(1) { for (i=0;i<20;i++) { 造盘→挖线索→'
        + '检查「恰好是这个难度」;成了就出去 } if (blackpc < 90) blackpc += 5; }`。两个要害——'
        + '**每个黑格比例只给 20 次机会**,不中就把比例 +5;而**爬升封顶在 90**(`blackpc < 90` 才加),'
        + '到顶之后原地无限重试,再没有兜底。所以「回不来」= 爬升路上每一档的 20 次都没中,'
        + '且终点那一档的命中率低到实际等于零。',
      '**栽的是哪一关,数出来了**:把 `lightup.c` include 进探针复刻内层一轮,固定时间预算里计数——'
        + '卡住的组合里「唯一解」那一关几乎 100% 通过,栽的全是「低一档解不出来」(1598)。'
        + '2×2 在 90% 黑格上 10 秒试了 **1234 万个布局:全部够好,一个都不够难**;20×2 2-way mirror Hard 是 167 万个,同样一个都不够难。'
        + '一句话:**盘子太窄,Easy 的推理就解完了一切,更难的推理根本没有用武之地**('
        + '和 Slant 上游注释里「边长 1 做不出 Hard」是同一回事,只是 Light Up 没在 validate_params 里挡)。'
        + '定下限这一轮又数了七组(90% 黑格,8 秒):3×3 的 None / 2镜 / 4镜 Tricky 分别 334 / 398 / 419 万个布局,'
        + '4×4 None Hard 152 万、5×4 None Hard 121 万、5×5 2镜 Hard 106 万、5×5 4镜 Hard 158 万——**够格的都是 0 个**。'
        + '唯一的反例是 4×4 4镜 Tricky:只有 15 个布局,里头就有 1 个够格,所以它反而稳。'
        + '也就是说「掉到 90 档」等于真死循环,两个千分之三的种子单独跑满 10 分钟确实没回来。',
      '**它是一条概率带,不是一条线**:爬升路上一共只有 15 档 × 20 次 = 300 次机会,'
        + '全空就掉到 90 那一档永远转。所以挂率 = 各档 (1 − 该档命中率) 的 20 次方连乘,'
        + '**只要命中率不是 1 就永远大于 0**——下限只能是挑一个可接受的挂率,不存在一条干净的线。'
        + '黑格 20%,按「短边」逐条数(挂数/种子数)。**短边比下限小一格**:'
        + 'Tricky 的 3×3 4镜 5/5、3×3 4旋 5/5、3×3 2镜 11/300、3×3 2旋 5/100;'
        + 'Hard 的 4×4 4镜 5/5、4×4 4旋 5/5、4×4 2旋 3/30、4×4 2镜 1/30、3×3 None 3/30、3×6 None 1/30。'
        + '**踩在下限上**:Tricky 五种对称整条(短边固定、长边 s / s+1 / s+3 / s+7 / s+15 / 30 / 50 / 80,'
        + '两个朝向)全 0,近方格加到 300 种子也全 0;Hard 只剩 None 的 5×4 1/300 和 4镜的 6×5 1/300,'
        + '5×5 各模式各跑 1000 种子,2镜和 4镜的 Hard 各中 1 次,其余十三组全 0。'
        + '**Easy 一个都没挂过**(五种对称的短边 2 那一条,近方格 300 种子全 0)。',
      '**为什么对称只贵一格,不是翻倍**:按基本盘(set_blacks 594-631:2-way 是 宽 × ⌈高/2⌉、'
        + '4-way mirror 是 ⌈宽/2⌉ × ⌈高/2⌉、4-way rotational 是 ⌊宽/2⌋ × ⌈高/2⌉ 加中心格)推,'
        + '「基本盘自己得够大」会给出 2k−1 / 2k,Tricky 要 5~6、Hard 要 7~8,和实测的 4 / 5 差得远。'
        + '原因是难度判定跑在**整盘**上,不在基本盘上:整盘面积决定「够格的谜题存不存在」,'
        + '基本盘只决定「可抽的黑格布局有多少个」。3×3 的可抽布局是 None 510 / 2-way 62 / 4镜 15 / 4旋 3,'
        + '对应挂率 0 / 4~5% / 必挂 / 必挂;到 4×4 面积够了,4镜同样只有 15 个布局却是 0/300。',
      '**下限上还剩多少**:None Hard 的 5×4 1/300、4镜 Hard 的 6×5 1/300、2镜和 4镜 Hard 的 5×5 各 1/1000,'
        + '其余全 0。按上面那条连乘式它不可能归零;再收一格(None Hard 到 5、4镜 Hard 到 6)也只是把千分之三'
        + '压到千分之一,而发牌镜像已经把这类卡住变成一个可取消的对话框,所以停在这里。'
        + '另外 4-way 的 Easy 下限 3 是口径不是故障:上游只禁 2×2,2×3 / 3×2 实测 0/300,'
        + '但下限统一按「矩形短边」表达,2×2 不行就整条短边 2 都不要。',
      '**黑格比例封 90 是结构线**:91 起 `blackpc < 90` 不成立、爬升彻底停摆,生成不出来就死转——'
        + '20×20 Hard 在 95 / 97 / 99 / 100 上全挂,10×10 Tricky 在 98 起全挂;100 更是整盘全黑、一盏灯都放不下。'
        + '90 以内也不是白纸:20×20 2旋 Hard 的 30 种子挂数是 20% → 0、60% → 0、**70% → 5**、80% → 8、85% → 9、90% → 9。'
        + 'owner 取 5..90,知道 70 以上有一到三成会挂。要清干净就封到 60。',
      '**耗时**(黑格 20%、2旋,三种子平均,秒):Easy 20 = 0.018、50 = 0.50、80 = 2.8、**100 = 7.6**;'
        + 'Tricky 20 = 0.12、50 = 5.4、80 = 49、**100 = 159**;Hard 20 = 0.37、50 = 12.8、80 = 93、**100 = 260**。'
        + '对称对耗时几乎没影响(20×20 Hard 五种对称都在 0.37–0.47 秒)。',
      '**可读性比前两个宽松**:画布 (宽+1) 格(BORDER = 半格,2077-2082),黑格里的线索是单个数字 0..4,'
        + '字号 = 格边长的 **3/5**(2204,Slant / Black Box 是 1/2)。实测「字号 ≥ 8 px」那条线:'
        + '2560×1440 是 **90**、1440×900 是 53、390×844 手机是 28。上限 100 只差一点。',
    ],
  },
  {
    id: 'map', title: 'Map', file: 'map.c', tuned: true,
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽',
        u: ['宽高都 ≥ 2(261)', '宽 ≤ INT_MAX/2/高(263,防溢出)'], f: '3..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高',
        u: ['同上,一条判两个', '区域数 ≥ 5 且 ≤ 面积 ⇒ 面积 ≥ 5——265 和 267 合起来只挡得住 2×2'],
        f: '3..100', d: '主', c: 'cap' },
      { label: 'Regions', kind: 'int', sem: '区域数', u: ['≥ 5(265)', '≤ 宽×高(267)'],
        f: '5..min(1000, 宽×高)', d: '看宽高;缩小棋盘时被夹', c: 'local' },
    ],
    notes: [
      '**定下来的**:宽高 3..100、区域数 5..min(1000, 面积),**四档难度共用一套表**。'
        + '难度是 choices(52-56:Easy / Normal / Hard / Unreasonable),不用申报表。'
        + '上游的 `validate_params`(259-270)没有 `if (full)` 分支,四条在任何路径上都查;'
        + '`decode_params` 省略 `n` 时补 `w*h/8`(188-191),面积不到 40 补出来的值会被 265 直接拒。',
      '**生成循环和它的逃生门,在 1442-1596**:`while(1) { genmap → gengraph → fourcolour → '
        + '按 params->diff 逐个删线索 → 若「低一档也能解」就重造 }`。逃生门**有条件**:只在 '
        + '`mindiff > 0 && (n < 9 || n > 2*wh/3)`(1589)时 `tries--`,50 次用完把 mindiff 压到 0'
        + '(= Easy)。**窗口之外没有出口**,够不着要的难度就一直转。Easy(diff 0)那道门是 '
        + '`map_solver(…, −1)`,等于「不做任何推理就已经解开」,对刚删过线索的盘几乎不可能成立,'
        + '所以 Easy 永远不被这道门挡住——实测七组最坏的格子各 10 种子全部一次过。',
      '**耗时的大头是 `fourcolour`,和难度无关**:791 → `fourcolour_recurse`(693)是带'
        + '「最受约束顶点」启发式的随机回溯,`assert(retd); /* by the Four Colour Theorem :-) */` '
        + '只保证解**存在**,不保证搜索快。逐轮拆过一次(30×30 n=200 种子 10,13 轮共 79.5 秒):'
        + '第 7 轮一轮 **80.1 秒**,其中 fourcolour **80.111**、genmap 0.002、gengraph 0.000、'
        + '删线索的 200 次求解合计 0.005、难度门 0.000。它在难度逻辑之前调用,所以 Easy 同样中招。',
      '**挂率随区域数爬,驱动量是短边、不是面积**(真 `new_desc`,40 种子;Easy 一轮就过,'
        + '所以 Easy 的挂率就是每张地图的爆炸概率 p)。取最差盘形:n ≤ 100 时 ≤ 2.5%、150 一成、'
        + '200 四分之一、300 四成半、500 八成。短边的作用——固定面积 900、固定 n=300,短边 '
        + '9 / 15 / 20 / 30 的挂率是 **0% / 2.5% / 10% / 12.5%**;反过来固定短边、长边翻两三倍'
        + '几乎不动(20×45 是 10%,20×100 是 7.5%)。4×100 和 9×100 在 n=50..500 上全是 0/40。'
        + '难度会放大 p:同一格 30×30 n=200,Easy 7.5% → Hard 40%,按 P = 1−(1−p)^轮数 反推约 6.5 轮。',
      '**小区域数是另一种病:整条分布平移,不是长尾**。难度在小图上不可达,循环要转很多轮,'
        + '而每轮成本 ∝ 面积。100×100 Hard 40 种子:n=12 中位 **9.25 秒**、27.5% 超 30 秒;'
        + 'n=15 中位 1.88;n=20 中位 0.77;n=30 中位 0.22。同一格的 Easy 中位 **0.012 秒**——'
        + '证明低端的慢是重试循环,不是 fourcolour。再往下是悬崖:n ≤ 8 逃生门开、静默降成 Easy;'
        + 'n=9/10/11 真挂(4×4 n=9 一百万轮不出,20×15 n=9 Hard 7/10,4×8 n=10 3/5,4×6 n=11 3/10)。',
      '**宽高下限取 3 的依据:两行的盘不是四色谜题**。回溯判色数,每格 300 张地图——'
        + '2×3 n5、2×5 n5/n10、2×10 n5/n10/n20、2×50 n30、2×100 n50 共 **2400 张,色数没有一次是 4**。'
        + '机制:用到第四色要图里有 K4(一个区域被三个互相相邻的区域围住),两行的条带做不出这个包围'
        + '(`gengraph` 只算正交相邻、不算对角,而且每条边存两份,所以 ngraph 是无向边数的 2 倍)。'
        + '短边 3 起才有:3×4 n6 4%、3×5 n8 6.7%、3×10 n15 10.7%、5×5 n12 17%、6×6 n12 38.3%、'
        + '20×15 n12 71.7%。3×3 仍然 0%(n=5 和 n=9 各 300 张),所以短边 3 没把退化盘清干净。',
      '**四档共用一套表是 owner 定的,明知留着三类坏格**:(一)n=9/10/11 配难度 > Easy **真挂**,'
        + '而且紧贴 Regions 滑块底端;(二)短边 3 配难度 > Easy 真挂(3×10 Hard 的 n=9 和 n=20 都是 5/5,'
        + '3×12 Hard 的 n=11 是 10/10);(三)n ≤ 8 和 n > ⌊2·面积/3⌋ 静默降成 Easy,界面上没有提示。'
        + '换来的是切难度不会把参数挪走、Width 和 Height 都是常量表。要清干净得按难度分档:'
        + '难度 > Easy 时短边 ≥ 4、高 ≥ max(4, ⌈30/宽⌉)、区域数 20..⌊2·面积/3⌋——那一圈实测是干净的'
        + '(5×6 n20、4×8 n20、6×6 n20 的 Hard 和 Unreasonable 各 0/40,最慢 2.2 秒)。',
    ],
  },
  {
    id: 'loopy', title: 'Loopy', file: 'loopy.c',
    params: [
      { label: 'Width', kind: 'int', sem: '网格宽(单位随网格类型)',
        u: ['每种网格有两个下限(281-300 的 GRIDLIST):两边都 ≥ amin(704),至少一边 ≥ omin(707)',
            'grid.c 里各网格的 grid_validate_params_* 只防溢出,100 以内不起作用'],
        f: 'amin..100;Penrose (kite/dart) 从 4 起,Penrose (rhombs) 从 5 起', d: '看「Grid type」', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '网格高', u: ['同上'], f: 'amin..100;宽 < omin 时从 omin 起;两种 Penrose 同宽的下限', d: '看网格类型和宽', c: 'cap' },
    ],
    notes: [
      'amin/omin 表:Squares 3/3、Triangular 3/3、Honeycomb 3/3、Snub-Square 3/3、Cairo 3/4、Great-Hexagonal 3/3、Octagonal 3/3、Kites 3/3、Floret 1/2、Dodecagonal 2/2、Great-Dodecagonal 2/2、Penrose (kite/dart) 3/3、Penrose (rhombs) 3/3、Great-Great-Dodecagonal 2/2、Kagome 3/3、Compass-Dodecagonal 2/2、Hats 6/6、Spectres 6/6。',
      '两种 Penrose 在上游放行的最小尺寸附近生成不正常:原生 oracle 五个种子各跑 10 秒,kite/dart 宽 3 全部超时、宽 4..7 配高 3 超时;rhombs 宽 3、4 全部超时,宽 5 配高 3、4 超时,宽 6..9 配高 3 超时(4×9 用 120 秒能出来,是极慢不是死循环);浏览器里探测另撞到过除零和 dsf 断言,引擎当场死掉。所以两边各抬到 4 / 5,见第四节。',
    ],
  },
  {
    id: 'inertia', title: 'Inertia', file: 'inertia.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 2(208)', '面积 ≥ 6(219)'], f: '2..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '2..100;宽 2 时从 3 起', d: '看宽', c: 'cap' },
    ],
  },
  {
    id: 'tents', title: 'Tents', file: 'tents.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 4(414)'], f: '4..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '4..100', d: '—', c: 'cap' },
    ],
  },
  {
    id: 'bridges', title: 'Bridges', file: 'bridges.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 3(813)'], f: '3..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '3..100', d: '—', c: 'cap' },
    ],
    notes: ['难度、桥数上限、岛屿占比、扩展因子在上游本来就是下拉,不在本次范围内。'],
  },
  {
    id: 'unequal', title: 'Unequal', file: 'unequal.c',
    params: [
      { label: 'Size (s*s)', kind: 'int', sem: '拉丁方阶数',
        u: ['3..32(269)', 'Adjacent 模式且难度 ≥ Tricky(下标 2)时 ≥ 5(273)'],
        f: '3..32;Adjacent 且难度 ≥ Tricky 时 5..32', d: '看「Mode」「Difficulty」', c: 'up' },
    ],
  },
  {
    id: 'galaxies', title: 'Galaxies', file: 'galaxies.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 3(330)'], f: '3..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '3..100', d: '—', c: 'cap' },
    ],
  },
  {
    id: 'filling', title: 'Filling', file: 'filling.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['≥ 1(188)'], f: '1..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['≥ 1(189)'], f: '1..100', d: '—', c: 'cap' },
    ],
  },
  {
    id: 'keen', title: 'Keen', file: 'keen.c',
    params: [
      { label: 'Grid size', kind: 'int', sem: '拉丁方阶数', u: ['3..9(227)'], f: '3..9', d: '—', c: 'up' },
    ],
  },
  {
    id: 'towers', title: 'Towers', file: 'towers.c',
    params: [
      { label: 'Grid size', kind: 'int', sem: '拉丁方阶数', u: ['3..9(250)'], f: '3..9', d: '—', c: 'up' },
    ],
  },
  {
    id: 'singles', title: 'Singles', file: 'singles.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 2(265)', '≤ 62(267:10+26+26 个可用符号)'], f: '2..62', d: '—', c: 'up' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '2..62', d: '—', c: 'up' },
    ],
  },
  {
    id: 'magnets', title: 'Magnets', file: 'magnets.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽',
        u: ['宽高都 ≥ 2(238-239)', 'Tricky(下标 1)时宽或高 ≥ 5(242-244)', 'Easy 时宽或高 ≥ 3(246-247)'],
        f: '2..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'],
        f: '2..100;宽不够时从 3(Tricky:5)起', d: '看宽和「Difficulty」', c: 'cap' },
    ],
  },
  {
    id: 'signpost', title: 'Signpost', file: 'signpost.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 1(432-433)', 'full:不能 1×1(436)'], f: '1..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '1..100;宽 1 时从 2 起', d: '看宽', c: 'cap' },
    ],
  },
  {
    id: 'range', title: 'Range', file: 'range.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 1(921-922)', '宽 + 高 − 1 ≤ 127(923:格数类型是 signed char)', 'full:2×2、1×2、2×1、1×1 生成不了(927-930)'],
        f: '1..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'],
        f: '1..min(100, 128−宽);宽 ≤ 2 时去掉 1 和 2', d: '看宽;宽变大时被夹(宽 100 时高最多 28)', c: 'up' },
    ],
  },
  {
    id: 'pearl', title: 'Pearl', file: 'pearl.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 5(288-289)', 'Tricky(下标 1)时宽 + 高 ≥ 11(294)'], f: '5..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '5..100;Tricky 且宽 5 时从 6 起', d: '看宽和「Difficulty」', c: 'cap' },
    ],
  },
  {
    id: 'undead', title: 'Undead', file: 'undead.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 3(216-217)', '宽 ≤ ⌊54/高⌋,即面积 ≤ 54(218)'], f: '3..18', d: '主', c: 'up' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '3..⌊54/宽⌋', d: '看宽;宽变大时被夹', c: 'up' },
    ],
  },
  {
    id: 'unruly', title: 'Unruly', file: 'unruly.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽(必须是偶数)', u: ['宽高都是偶数(289)', '≥ 6(291)'], f: '6, 8, …, 100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高(偶数)',
        u: ['同上', '「Unique rows and columns」时:宽 2n 则高 ≤ A177790[n],反之亦然(295-323);表到 n = 23 为止,更宽不设限'],
        f: '6, 8, …, 100;unique 时按表夹:宽 6 → 高 ≤ 14,宽 8 → ≤ 34,宽 10 → ≤ 84,宽 ≥ 12 不限;同时高 6 → 宽 ≤ 14 等', d: '看宽和开关', c: 'cap' },
    ],
  },
  {
    id: 'flood', title: 'Flood', file: 'flood.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['面积 ≥ 2(220)', '宽高都 ≥ 1(222)'], f: '1..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '1..100;宽 1 时从 2 起', d: '看宽', c: 'cap' },
      { label: 'Colours', kind: 'int', sem: '颜色数', u: ['3..10(226,MAXCOLOURS)'], f: '3..10', d: '—', c: 'up' },
      { label: 'Extra moves permitted', kind: 'int', sem: '在最优步数之上额外允许的步数', u: ['≥ 0(228)', '无上限'], f: '0..100', d: '—', c: 'cap' },
    ],
  },
  {
    id: 'tracks', title: 'Train Tracks', file: 'tracks.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 4(202)'], f: '4..100', d: '—', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['同上'], f: '4..100', d: '—', c: 'cap' },
    ],
  },
  {
    id: 'palisade', title: 'Palisade', file: 'palisade.c',
    params: [
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['宽高都 ≥ 1(169-170)'], f: '1..100', d: '主', c: 'cap' },
      { label: 'Height', kind: 'int', sem: '棋盘高', u: ['区域大小 < 面积 ⇒ 面积 ≥ 2'], f: '1..100;宽 1 时从 2 起', d: '看宽', c: 'cap' },
      { label: 'Region size', kind: 'int', sem: '每个区域的格数 k',
        u: ['≥ 1(168)', '整除面积(174)', 'full:≠ 面积(178)', 'full:k = 2 只在宽或高为 1 时可以(181)'],
        f: '面积的约数,去掉面积本身;宽高都不是 1 时再去掉 2。例:5×5 → {1, 5};8×6 → {1, 3, 4, 6, 8, 12, 16, 24}', d: '看宽高;棋盘变了吸到最近的约数', c: 'up' },
    ],
    notes: ['表不连续,滑块按表的下标走,每一档都是合法约数。'],
  },
  {
    id: 'mosaic', title: 'Mosaic', file: 'mosaic.c',
    params: [
      { label: 'Height', kind: 'int', sem: '棋盘高(上游把高排在宽前面)', u: ['宽高都 ≥ 3(242)', '面积 ≤ 10000(245,MAX_TILES)'], f: '3..100', d: '—', c: 'cap' },
      { label: 'Width', kind: 'int', sem: '棋盘宽', u: ['同上'], f: '3..100', d: '—', c: 'cap' },
    ],
    notes: ['100×100 = 10000 恰好等于上游的 MAX_TILES,两个封顶重合。'],
  },
]

// 与上游的出入:本仓库故意比上游窄(或补了上游漏掉的)的地方。
export const DEVIATIONS = [
  { game: 'twiddle', what: '宽高封到 50', why: '同 Fifteen(字号 = 格边/3)。旋转块边长不收,只跟上游的 n ≤ min(宽, 高)——顶上几档很贵(默认打乱是 O(w·h·n⁴)),耗时留到第三轮。' },
  { game: 'sixteen', what: '宽高封到 50,打乱步数封到 100', why: '宽高同 Fifteen(字号 = 格边/3)。打乱步数:它的用途是「数出这几步再倒回去」,不是打乱——真要打乱用 0(默认值)。实测 50×50 要五千步以上才追平 m=0,中间那一段两头不靠。上游只要求 ≥ 0。' },
  { game: 'fifteen', what: '宽高封到 50', why: '格子里要写编号,字号是格边的 1/3;50×50 在 2560×1440 上是字号 8 px,四位数刚好读得出来,再大就是一片灰点。上游只要求 ≥ 2。' },
  { game: 'cube', what: '三角格(四面体 / 八面体 / 二十面体)两维封到 50,方格(立方体)封到 100', why: '六边形跨 d1+d2 行,50×50 的铺展和方格 100×100 相当;再往上每走一步的重画就从 220 ms 起跳(带动画,O(面积)),格子也掉到 4 px 以下。' },
  { game: 'net', what: '宽高从 3 起', why: '上游只禁 1×1、和 wrap+unique 下的 2;1×n 是一条直管,不成谜题。下限抬到 3 之后上游那两条永远触发不了,宽和高之间也不再互相依赖。' },
  { game: 'dominosa', what: '最大点数封顶 98', why: '棋盘宽 n+2 ≤ 100;上游只防溢出。' },
  { game: 'lightup', what: '宽高下限按对称 × 难度查表:None 2/3/4、2-way 2/4/5、4-way 3/4/5(Easy/Tricky/Hard)', why: '上游只查 ≥ 2(4-way 另禁 2×2,4-way rotational 另要方盘)。窄盘上难度 ≥ Tricky 时生成器回不来:「低一档解不出来」那道门(1598)永远过不去,而爬黑格比例的兜底封顶在 90(1609),到顶就原地死转——90 那一档数过上百万个布局,够格的是 0 个。对称把可抽的黑格布局砍掉大半,那道门更难过,所以四种对称都比 None 高一格。4-way 的 Easy 3 是口径:上游只禁 2×2,2×3 实测 0/300,但下限统一按矩形短边表达。' },
  { game: 'lightup', what: '黑格比例封到 90', why: '上游给到 100。91 起 `blackpc < 90` 不成立、爬升彻底停摆,生成不出来就死转(20×20 Hard 在 95 以上全挂);100 更是整盘全黑、一盏灯都放不下。' },
  { game: 'map', what: '宽高从 3 起', why: '上游放行 2:265 和 267(n ≥ 5 且 n ≤ 面积)合起来只挡得住 2×2。但两行的盘永远用不到第四种颜色——2×3 / 2×5 / 2×10 / 2×50 / 2×100 各 300 张、共 2400 张地图,色数没有一次是 4。用到第四色要图里有 K4(一个区域被三个互相相邻的区域围住),两行的条带做不出这个包围,所以它不是四色谜题,调色板上第四个键是死键。3 起之后面积恒 ≥ 9,上游那条再也卡不到宽高,宽和高之间不再互相依赖。' },
  { game: 'map', what: '区域数封到 1000', why: '上游给到面积(100×100 是 10000)。挂率随区域数爬(真 new_desc,40 种子,取最差盘形):≤ 100 时 ≤ 2.5%、150 一成、200 四分之一、300 四成半、500 八成。根子是 fourcolour 的随机回溯偶尔炸掉一轮(实测单轮 80 秒),和难度无关。1000 是 owner 暂定的档,不是安全线。' },
  { game: 'solo', what: '行数从 1 起', why: '上游 validate_params 没查 r 的下限,0 和负数放行,new_game_desc 才出事;r = 1 是 Jigsaw 布局的定义值。' },
  { game: 'solo', what: '没勾 Jigsaw 时行数从 2 起,列数封到 order/2', why: '上游 r = 1 即 Jigsaw(solo.c:471)。每个控件各自提交之后,勾掉 Jigsaw 送回去的仍是 r = 1,勾选框会自己弹回来,普通棋盘就到不了了;行数从 2 起,settle 在勾掉时把 1 抬成 2。' },
  { game: 'solo', what: '二阶(2j、2×2)配 4 向旋转 / 4 向镜像 / 8 向镜像,及二阶 Killer,去掉', why: '上游放行但生成不终止:原生 oracle 五个种子各 10 秒全部超时(其它对称、三阶起全部正常)。' },
  { game: 'loopy', what: 'Penrose (kite/dart) 宽高从 4 起,Penrose (rhombs) 从 5 起', why: '上游 amin 3。最小尺寸附近生成极慢(五个种子 10 秒全超时,4×9 要 120 秒)且浏览器里撞到过除零 / dsf 断言,引擎当场死掉;取一个把超时格全盖住的矩形。' },
  { game: 'blackbox', what: '「最多球数」≤ 宽×高−1', why: '上游只查最少 < 格数;最多超过格数时放球循环永不结束。' },
  { game: 'pattern', what: '宽高只给 5 的倍数 5..50', why: '上游只查 > 0 和面积 ≥ 2。生成是拒绝采样,短边 ≤ 4 时「不许整行纯色」那条几乎必然触发(3×30 跑满 300 秒不出结果),面积大时求解器那条通不过(面积每多 100 格耗时翻倍);上限 50 另有文字线索的可读性一条。步长 5 是 owner 定的档位。' },
  { game: 'rect', what: '宽高从 2 起', why: '上游放行 1×n(只查 w·h ≥ 2),但钳位写的是 `bw < 2 && w >= 2`,宽是 1 时 base 边长留在 0,扩展因子一动就 wasm abort(random.c:275)。' },
  { game: 'rect', what: '扩展因子换成 base 对的枚举,e 由它算出', why: 'e 只通过 (base_w, base_h) 影响生成,同一对 base 的局面逐字节相同,连续的 e 大半是空拖。枚举之后每一档都换局面;档数随棋盘变,最多 195(100×99)。上游只要求非负。' },
  { game: '(浮点)', what: '障碍概率步长 0.01、扩展因子按 base 对枚举', why: '滑块只能取有限个值;上游接受任意小数。Game ID 载进来的表外小数,第一次落定时吸到最近一档(Rectangles 例外:回到第一档)。' },
  { game: 'mines', what: '宽高从 4 起', why: '上游放行 3×4(勾了唯一解时两维 > 2、面积 ≥ 10)。但 3×3 面积不够放雷,3×n 一到高密度唯一解就修不出来(3×100 撒 30% 跑不完)。4 起面积恒 ≥ 16。' },
  { game: 'mines', what: '不再接受「20%」写法', why: '滑块只出绝对雷数,占比作为读数显示;可达的雷数集合不变。上游那条只换算不钳位,超了直接报错。' },
]
